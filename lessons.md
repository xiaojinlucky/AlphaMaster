# Lessons

- 2026-07-14：AlphaMaster 的远端安装、调试、测试和 `sbatch` 提交不得在 `login-node` 执行。每批远端操作前使用 `D:\Desktop\codex-remote-tools\check-best-node.ps1` 在 `compute-node-11/12/13` 中选择实时最优节点；`login-node` 仅作为 SSH `ProxyCommand` 跳板。提交所在节点不等于训练节点，训练资源与落点由 Slurm 调度器决定，不使用 `--nodelist` 固定节点。
- 2026-07-15：AlphaMaster 前端设计必须使用独立新建的 Stitch 私有项目；不得复用 `Quant Frontend Design System` 等旧期权/AOD 项目。任何 Stitch 创建或编辑动作前，必须先核对项目标题、项目 ID 和已有页面归属，并把 AlphaMaster 项目单独登记到 `D:\Desktop\Quant\前端设计\STITCH_REGISTRY.md`，避免跨项目污染。
- 2026-07-15：要求 Stitch 生成中文界面时，不能只写“使用中文”等软约束。必须逐页处理，并把“除明确白名单外，所有用户可见文字必须为简体中文”设为硬验收条件；提供关键文案的中英映射，要求生成前后执行逐项语言自检，发现任何非白名单英文即继续修改而不是交付。视觉重排、业务约束和语言本地化不要在一次三页生成请求中混杂，避免语言要求被稀释。
- 2026-07-15：前端 Stitch 提示中必须严格分离“用户可见需求”和“后台实现/数据工程约束”。A股格式转换、只读源目录、训练副本路径、数据校验、Slurm、API/DOM/框架边界等后台任务不得被做成导航、卡片、页脚或帮助入口；除非用户明确要求暴露为前端功能。禁止 Stitch 自行添加版本号、产品定位、文档中心、回测逻辑等无来源内容；设计验收时必须逐项追溯每个可见元素的需求来源，无来源即删除。
- 2026-07-15：不得把备份文件中的 Stitch 配置或手工 HTTP 调用成功当成“Codex MCP 已配置”。必须同时核对当前生效的 `~/.codex/config.toml`、`codex mcp list --json` 中存在 `stitch`，并通过原生 MCP 成功枚举目标项目。Stitch 生成/编辑可能超过默认 60 秒，应显式设置合理的 `tool_timeout_sec`；为避免全局 MCP 故障阻塞所有新任务，优先使用 AlphaMaster 项目级 `.codex/config.toml`，修改后重启 Codex。
- 2026-07-16：GitHub 仓库即使是私有，也不能上传或保留明文 token、`.env`、原始行情/交易/训练数据、数据库、日志、运行态文件和模型 checkpoint。给外部模型的源码包必须排除 `.git`，从明确白名单生成，并逐项核对清单与 SHA-256；发现已进入历史的凭证时，当前版本先改为环境变量读取，旧凭证立即轮换，历史净化必须另行授权。
- 2026-07-16：未完成的大型候选实现可以进入专用审查包，但必须同时携带基准 commit、工作区差异、已知阻断项和测试证据，并明确标为“待审查”，不得为了交接方便直接包装成已完成版本或混入稳定主线。
- 2026-07-16：AlphaMaster 是完全独立的项目，核心二次开发目标是打通 Windows 本机控制端与 Linux Slurm 服务器的联通和交互。任何其他仓库的目标、上下文、源码、工单、验证、Git 提交、文件包和执行流程都不得混入；即使只是让外部模型统一规划，也必须分别建立任务和移交材料。
- 2026-07-16：判断数据兼容性时必须先用 Git 历史区分“原作者既有合同”和“fork 后新增合同”。`NVDA_M5.manifest.json` 这类 Parquet sidecar 并非原作者 `rosemarycox5334-debug/AlphaMaster` 的既有要求，而是 fork 后提交 `c3717be` 为 Slurm 远程训练新增的数据来源与完整性校验；不得把因此产生的兼容错误归因成旧数据格式错误。
- 2026-07-17：安全源码包不能只靠 `.gitignore`、目录名或敏感扩展名判断“无运行态”。`git archive` 会包含历史上已跟踪的 `training_time_XAUUSD.json`；打包必须对 Git 树执行显式风险文件审计和排除，并在解包后逐文件匹配与密钥扫描，不能把“未发现密钥”误写成“未包含运行态”。
- 2026-07-18：大模型供应商能力正在独立项目中先行修复，AlphaMaster 不重复开发或提交候选实现；等独立项目形成经过验证的结果后，再按 AlphaMaster 自身边界决定是否借鉴。当前 AM 优先审查会失真历史成绩的未来函数、回测语义和过拟合问题。
- 2026-07-23：规划 A 股实盘前必须先确认用户是否真正拥有可验证的券商量化账户；没有 QMT/PTrade 实盘渠道时，不把接口接入当作可执行主线，也不用模拟结果冒充实盘验证。先交付与券商无关、可真实运行的“已收盘 K 线交易信号 + 可审计事件账本 + 飞书推送”。
- 2026-07-23：AlphaMaster 的模型训练一律提交到用户已有服务器的 Slurm，不在 Windows 本机训练。推进大 A 时不能把信号功能单独交付后停下；必须用单节点资源申请边跑边监控“训练 → 回测 → 虚拟信号”，并行落实大 A 数据适配和前端。先以短真实任务跑通，再根据 `sacct` 的耗时、CPU 和内存证据逐步放大。
- 2026-07-24：AM 进入多标的推进后，训练是持续后台主线，不能因为调研、前端、代码审查或用户新指令而停止；本机 Web 监控进程必须持续观察当前单节点 Slurm job，任务结束后由批队列接续下一标的。所有新工作默认与训练并行，只有用户明确要求或作业自身失败才停止训练链。
- 2026-07-24：只要批次中仍有 Slurm 作业处于排队、运行或下载阶段，就禁止覆盖共享远端训练运行时，也禁止修改会进入 run manifest 的训练源码后继续派发同一批次。当前 50 只基线批次必须使用同一份冻结源码；前端、测试和文档可并行修改。后续若要边训练边升级训练源码，先实现按批次或按 run 隔离的不可变远端运行时，不能再复用会被覆盖的共享目录。
- 2026-07-24：导入 `web.app` 的测试不得触碰生产 `local_runs/training_queue.sqlite3`。凡直接调用后台监控函数，必须替换批队列控制器或使用隔离数据库；运行时代码也不得因一次空快照、测试替身或无关 run 就把已取得 Slurm job ID 的项目写成失败。恢复误报只能用完全一致的 `run_id + job_id` 原地对账，禁止重提作业。
- 2026-07-24：`xiaojinlucky/AlphaMaster` 必须持续保持公开，目的是让网页版 GPT 与 BioMNI（BIOMNI）直接读取已提交源码和证据；不得再把“改为私有”作为发布前提。公开不改变安全边界：每次仍只精确暂存已授权文件，提交前扫描密钥，并排除 `.env`、凭据、原始数据、数据库、模型、checkpoint、训练历史、日志和运行态。
- 2026-07-24：大 A 行情不自建腾讯、东财、同花顺或通达信网页抓取器，优先复用 Longbridge 官方 SDK、AKShare、easyquotation、efinance、pytdx/mootdx 和 pywencai/thsdk 的成熟能力。但“复用”不等于无痕自动回退：供应商、具体接口、复权、成交量单位、K 线时间语义和合同版本必须共同形成独立来源身份；换源必须新建数据集并重新训练，实时快照不得直接拼到复权历史。
- 2026-07-24：Longbridge A 股历史日线单次最多返回 1,000 根，但可按日期继续向前分页；其前复权最新价格可与 AKShare 前复权对齐，成交量单位是手而 AKShare 是股。高分红股票的长历史前复权价格可能为负，不能直接用于对数收益训练。Longbridge 与新浪后复权必须分成两套数据和模型，不能只做乘 100 或改来源标签后混用。
