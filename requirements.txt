# AlphaGPT MT5 - Python Dependencies
# Python 3.10+ required (recommended: 3.11)
#
# Installation:
#   pip3 install -r requirements.txt
#   # Or use: pip install -r requirements.txt (if pip points to Python 3)

# ===== Core Dependencies (Required for main pipeline) =====

# Deep Learning & Numerical Computing
torch==2.8.0          # Windows 2.12/2.13 c10.dll regression; 2.8 verified locally
numpy>=1.24.0         # Numerical operations

# Data Processing
pandas>=2.0.0         # Data manipulation and analysis
pytdx>=1.72           # Tongdaxin A-share realtime quotes
akshare==1.18.64      # A-share historical qfq training datasets
xlrd==2.0.1           # Official CSI constituent XLS reader

# MT5 Trading Platform
MetaTrader5>=5.0.45   # MetaTrader 5 Python API for data and order execution

# Environment & Configuration
python-dotenv>=1.0.0  # Load .env files

# Logging & Progress
loguru>=0.7.0         # Structured logging
tqdm>=4.66.0          # Progress bars

# Training Web UI
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ===== Optional Dependencies (For experimental scripts) =====
# See requirements-optional.txt for:
#   - matplotlib, seaborn (for lord/experiment.py visualization)
#   - tushare (for times.py Chinese stock market data)
