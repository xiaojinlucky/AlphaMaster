# WO-AM-04：000792 READY 与增量备份验收（2026-07-30）

## 结论

`000792` 已完成 9000 步训练，并通过训练产物合同、回测、虚拟信号、队列和增量备份验收。生产队列已自动推进 `000988`。本次未停止或重提作业，未修改 34 个冻结训练文件，未触碰 sealed OOS 或旧数据。

## 训练与身份

- run：`run_20260723T235959Z_1d99b294`
- Slurm：`582419`，cu05，`COMPLETED/0:0`，墙钟 `08:24:18`
- runtime commit：`bb280ca2dada4b087ce1bf5d86f1292b73c576b9`
- 34 个训练源文件聚合 SHA-256：`0e5ca8c3ee924b94757e453e01637d4e1a7f85c7ae9a50ad9baacebdf0564ab0`
- 数据 SHA-256：`6745686b748e12969f57b5c5c75be4e45362f9b6a18e11e82b03da87d87a3792`
- run manifest SHA-256：`f6e5e7cc1f776b0b625aebb05713fd8f8a4cf5aa21fc91d9177440c4840151c7`
- 结果 manifest SHA-256：`4369ba8224468f4dc111f38104a39ee3e26166f652e93a879e2341857a7167a3`
- 评分合同：`open_t1_t2_same_index_tail2_v1`
- 固定适配器 SHA-256：`fdddd12bf0ac1c1fa471807790533b386fb3a02216c9ab3b5c233eabc69373ce`

## 训练产物合同

使用已验收的 25 项只读合同审计框架，结果为 **25/0**。审计包含：

- run、symbol、job、runtime commit、训练数据和 34 个源文件身份闭合；
- Slurm 终态为 `COMPLETED/0:0`；
- 以本机固定适配器字节流远端重验原始 452 件产物；
- 原始结果 manifest SHA-256 为 `2dc8c5256fceb6f989f108e36b811b632a00e5c881b3a7fda87e2e4941939aeb`；
- 按 `highest_step_single_run_v1` 省略 449 个旧 checkpoint，只发布 step 9000 checkpoint、策略和训练历史 3 件；
- checkpoint 的模型、优化器、best snapshot、9000 步历史及策略语义均通过。

3 件归一化产物：

| 产物 | 字节 | SHA-256 |
|---|---:|---|
| `ckpt_000792_step_9000.pt` | 6,708,942 | `5148264a53f121b9ae4366b8b2be96a1bf742fb3d681c8432022a04f9ea6fd0e` |
| `strategies/best_000792.json` | 1,030 | `91a9297c5063ec9a2afa6dfa02aa6c51e5f1b370a9b3297a1d952750ea5d4d4f` |
| `training_history_000792.json` | 2,310,503 | `a272b0106fc2363838bbdb95d9bbf028fc957ea72564fb78f6b5a875c08c15b2` |

训练 best score 为 `2.0245869159698486`。

## 四层 READY

- 训练层：READY。
- 回测层：READY；报告 SHA-256 `966f50db44602efce704ee8cea068d2400a05fb227553632a65addabc7a8c53e`。
- 虚拟信号层：READY；输出 SHA-256 `9986ac6fbcccae49d00402b2398cd4e862fc75921ec91689719daa3538119d27`；动作 `BUY`，目标虚拟仓位约 `0.1323`，交付状态 `SKIPPED`，未发送飞书。
- 队列层：ordinal 3 / `000792` / `582419` / execution run 均绑定正确，状态 READY；队列 SQLite `integrity_check=ok`。

回测使用训练数据上的含成本 replay，`total_return=3.619935`、`Sharpe=0.9940`、`Sortino=2.6573`、`profit_loss_ratio=2.6193`。这些数字不是 sealed OOS 成绩。

## 增量备份与独立核验

只执行一次 `scripts/backup_am_assets.py`：

- 待传 1,288 个文件、3,218.5 MB，其中 SQLite 在线一致性快照 37 个；
- 用时 221 秒；
- 新 `CURRENT`：`gen-1785374003929226888-1112810-9d40a766076a531c5931cd45`；
- 当前 generation 含 1,290 个普通文件；
- 旧 generation 保留；无 `.building` 或 `.incoming` 残留。

独立备份审计结果为 **8/0**：

- 37/37 SQLite 均 `integrity_check=ok`、journal mode 为 DELETE、WAL/SHM 为 0；
- `000792` 的 17 个非 SQLite 持久文件逐 SHA-256 一致；
- signal SQLite 本机与备份端业务行一致；
- 队列快照为 `READY=4 / TRAINING=1 / QUEUED=45`，`000792` READY 绑定正确，活动项为 `000988`。

第一次复用 `000725` 审计基线时出现 2 个断言失败：旧脚本仍要求“只有 1 个 generation”并把目标 ordinal 固定为 2。只读实查确认新旧两个 generation 均完整保留，`000792` 的真实 ordinal 为 3；按本次冻结事实修正验收参数后为 8/0。该失败属于审计基线陈旧，不是生产或备份损坏。

## 对抗复核

独立只读对抗复核结论为 **ACCEPT，P0/P1/P2=`0/0/0`**。审查者独立重算并确认：

- 34 个源文件、runtime commit、评分合同和固定适配器身份一致；
- 原始 452 件、省略 449 件、归一化 3 件及 checkpoint/历史/策略语义一致；
- Slurm、四层 READY、Web 和当前队列状态一致；
- 新旧两个 generation 均保留，当前 generation 为 1,290 个普通文件、3,218,579,641 字节；
- 37/37 SQLite 和队列、signal 业务摘要一致；
- 17 个非 SQLite 持久资产的口径为 run 目录 19 个文件加发布指针 1 个，再排除 signal SQLite 主库/WAL/SHM 3 个。发布指针两端均为 1,997 字节，SHA-256 `0bf3684214c600549b8bbad8eb5ab417decfe0128ed7a6f24de15e61e8230925`。

## 自动推进

核验时活动项为 ordinal 4 / `000988`，在 cu05 RUNNING，12 CPU，训练到
step 197/9000，best score `1.4231030941009521`，`slurm.err` 为 0 字节。
活动 run/job 身份不进入公开快照；runtime commit、评分合同和冻结源码
SHA 均未漂移。
