# WO-AM-04：000725 READY 与首次完整版本备份验收

日期：2026-07-30
结论：`000725` 训练产物合同、后处理四层和首次生产完整版本备份均通过。

## 1. 训练与运行身份

- symbol：`000725`
- run：`run_20260723T235959Z_15080906`
- Slurm job：`581904`
- Slurm 终态：`COMPLETED / 0:0`
- 节点与墙钟：`cu01 / 06:15:01`
- runtime commit：
  `bb280ca2dada4b087ce1bf5d86f1292b73c576b9`
- 34 个冻结训练源文件聚合 SHA-256：
  `0e5ca8c3ee924b94757e453e01637d4e1a7f85c7ae9a50ad9baacebdf0564ab0`
- scoring contract：`open_t1_t2_same_index_tail2_v1`

远端 `.slurm_job.json` 的 job/run/资源绑定、远端输入 manifest SHA-256
`eddb3c8a4685cd21b0cb715ec8227b0e051735eb28d07b563d30366936117682`
和 result manifest 中的 run/job/runtime/scoring/source files 均一致。

## 2. 原始产物与归一化产物

远端原始 result manifest：

- SHA-256：
  `350b4202b56b97afd5a384c39df8b6e5fe7c9669feb6ac6011f892ae18317151`
- 原始产物：452 件
- 最大 checkpoint step：9000

固定 SHA 适配器先逐件验证原始 manifest 声明的路径、大小和 SHA-256，再按
`highest_step_single_run_v1` 发布 3 件：

1. step 9000 checkpoint：
   `0955fe77af7a66a082c44ae640f3b1db59379e121ce9842f4ca7d7c0da3c7f18`
2. 策略：
   `db0bb646104fbb6ec189cb5069cc9fa4096f19ba2664f934c4e4d28b56583660`
3. 9000 步训练历史：
   `61d02dffb13ee50150000a2956da4477fe46e298b2973904fd3dc72f4ff8ac58`

本机归一化 result manifest SHA-256：
`88abb26b9c4819df701d40c62eb068dbbd8f0ec7e338b0c267d84a26f5676194`。
固定适配器实际 SHA-256 与 pin 均为
`fdddd12bf0ac1c1fa471807790533b386fb3a02216c9ab3b5c233eabc69373ce`。

checkpoint 使用 `torch.load(weights_only=True)` 独立加载通过：step=9000，
symbol/timeframe/data/scoring identity 全对，模型、优化器、best snapshot 和
训练历史均存在；历史 step 精确为 0～8999。训练 best score 为
`1.667362928390503`。

## 3. 四层 READY

- training：READY
- backtest：READY
- signal：READY
- queue：READY

回测使用训练数据回放，不是 sealed OOS；含成本结果为 total return
`2.911953`、Sharpe `1.2393`、Sortino `1.9082`、profit/loss ratio
`1.4544`。虚拟信号原始方向为 SHORT，但当前空仓生命周期动作是 HOLD。

本机只读验收脚本
`scratch/goal_am_custody/audit_ready_000725.py` 为 25 通过、0 失败；
它使用固定 adapter 同字节流在远端重新读取并校验原始 452 件，不依赖
manifest 的自我声明。

训练产物独立对抗复审最终 `ACCEPT`，P0/P1/P2=`0/0/0`。

## 4. 首次生产完整版本迁移

使用 SHA-256
`5c793f9cae70058ea37c362154a723f5621b70fc5326583aa80b1c0f4d6605d7`
的 `scripts/backup_am_assets.py` 执行：

- 待传：1,268 个文件
- 体积：3,202.5 MB
- SQLite 在线快照：36 个
- 耗时：201 秒
- 发布后当前版本文件数：1,270

`CURRENT` 当前指向：

`gen-1785346860961917693-1491571-882c19b7a4b2333e758804dd`

独立验收确认：

- 备份根、`.generations`、`CURRENT` 和当前 generation 类型正确，均非符号链接；
- 当前 generation 已闭合，无 `.building`，`.incoming` 为空；
- 旧顶层 `checkpoints/local_runs/published_training` 原样保留；
- 36/36 SQLite 均 `integrity_check=ok`、`journal_mode=delete`；
- 每库配套 WAL/SHM 均存在且为 0 字节，用于中和旧日志；
- `000725` 的 17 个非 SQLite 持久文件本机/远端 SHA-256 全部一致；
- signal SQLite 的两张业务表逐行与本机在线库一致；
- 队列快照为 `READY=3 / TRAINING=1 / QUEUED=46`，`000725` 绑定
  job 581904，活动项为 `000792` / job 582419。

只读验收脚本 `scratch/goal_am_custody/audit_backup_000725.py`
为 8 通过、0 失败。

备份独立对抗复审最终 `ACCEPT`，P0/P1/P2=`0/0/0`。

执行期间启动器被误触发第二次。后启动副本在独立 staging 内被终止，
tar/ssh 非零后失败关闭；它没有移动 `CURRENT`，对应 staging 已清理，
`.incoming` 与 `.building` 最终均为空。正式发布只来自先启动的成功进程。

## 5. 安全边界

- 未停止或重提训练作业；
- 未修改 34 个冻结训练文件；
- 未触碰 sealed OOS；
- 未修改旧数据；
- 未自动重训。

队列已自动推进 `000792` /
`run_20260723T235959Z_1d99b294` / Slurm `582419`，核验时在 cu05
RUNNING。后续继续按同一合同逐件验收与只新增备份。
