# AlphaMaster — 问题、需求、修改意见与完成记录

## 2026-07-25 free-stockdb 全量下载

- 用户授权 G 盘最多 1 TB 用于数据，并要求立即下载当天可取得的全部数据；
  只有出现真实额度限制时才优先保沪深 300。
- 已完成 `v0.2.1-more-power` 全量同步，最终快照为
  `10,181,938,798` 字节、293 个 `.ldb`、0 个 `.part`；296 个实际
  落盘文件全部通过服务器清单 SHA-256 校验。
- 本地查询确认：7,563 个可查询代码共有 18,465,538 条日线和
  270,210,217 条 1 分钟线。日线每个代码至少有 1 条；分钟线有 7,171
  个代码至少有 1 条，其中 7,170 个最后交易日为 `2026-07-24`，另 1 个
  为 `2026-07-17`。起止键尚不能证明区间内逐日、逐分钟无缺口。
- 该同步不走 2,000 次在线测试额度，因此未启用沪深 300 在线兜底。
- 数据保存在 `G:\QuantData\free-stockdb\package\stockdb\data`，未进入
  AlphaMaster Git；更新器、监控器和数据库进程均已退出。
- 当前 300 只沪深300成分的规范 D1 v2 位于
  `G:\QuantData\free-stockdb\am_exports\20260724_csi300_am_inputs_v2`：
  273 只通过 AlphaMaster 训练合同，27 只因历史不足、缺前复权因子或
  前复权连续性异常而隔离。项目新增独立来源
  `ashare_free_stockdb_qfq`，不允许冒充 `ashare_local` 或 AKShare。
  这 273 只只允许做单股探索训练和工程验证；正式历史沪深300组合回测
  仍缺 649 个历史成分，并且当前前复权连续性尚未完成独立权威源复核。
- FreeStockDB sidecar 现在以固定文件名、SHA-256、大小和完整来源 payload
  随 Parquet 上传；远端控制器和 Worker 再次校验后，原子物化到数据文件
  旁供统一严格 loader 读取。来源身份继续进入 result/published manifest，
  不再出现“本机预检通过、远端因缺 sidecar 必然失败”的断链。
- 在线接口实测只能证明“测试期累计超过 2,000 次后被阻断”；没有证据
  支持“每天 2,000 次”、每日重置或一分钟线另有独立额度。作者
  2026-07-23 新增 43 个全量接口，明确要求至少使用当日版本，并要求这些
  在线接口只作补充查询、不要批量扫描。当前归档
  `free-stockdb-windows-v0.2.1-more-power.zip` 为 3,910,405 字节，
  SHA-256 是
  `07e6b2eefd92e9c1a7cf2ecfb3fc80ea87ad97e4a4dce58d8486415100ab03fd`，
  与作者 GitHub Latest Release 资产逐字节一致，无需重新下载或覆盖；
  未来升级需先在隔离目录比对和验证。

## 2026-07-24 卡死任务续接

- 用户确认继续两条并行工作：服务器单节点公式评估串并行基准，以及普通
  A 股虚拟执行约束层；正式 A50 训练队列必须原样持续运行。
- 已修复旧 Slurm job 离开 `squeue` 后本机永久卡住的问题，并覆盖 result
  manifest 短暂不可见的竞态；不会因此重复提交。
- 已完成确定性执行内核和对抗修复。执行决策绑定账户现金、逐批持仓和决策日
  估值行情；不重选替代股票，不猜停牌、涨跌停或费率。
- 已完成同 12 核、同公式、同数据、五轮位置平衡的基准。2×6 仅快约 2.5%，
  其他并行模式更慢，因此不把线程并行写入生产训练循环。
- 明确未完成：生产执行账本、组合回测共用执行器、组合飞书摘要和任何券商发单。

## 1. 用户目标

- 安装并长期维护自己的公开 AlphaMaster fork；公开是为了让网页版 GPT 直接读取仓库。
- 在原项目上做轻量二次开发，不做无关的大规模重写。
- 使用 Windows 本机选择/准备数据，通过 Slurm CPU 完成训练并自动取回可验证产物。
- 打通本机与服务器的联通和交互，包括上传、提交、状态、日志、取消、恢复、下载和结果校验。
- 统一 01 模型训练、02 策略回测、03 实时分析的前端视觉。
- 前端采用大字体、舒展排版、清晰边框和一致的侧边栏/卡片/控件。
- 旧 MT5/OKX 数据与未来新增数据能便捷进入训练和回测，而不要求用户手写 manifest。
- 回测能够独立选择测试数据，并严格区分训练集重放、样本外测试和重叠诊断。
- 项目内容、规则、问题、需求、完成状态和验证证据提交到公开 GitHub，供网页版 GPT 直接读取并充当规划、调研和工单审查者。
- 当前二次开发延续 `AlphaGPT + StackVM + 固定仓位映射 + 回测奖励` 的核心训练边界，同时向大 A 多标的龙头轮动扩展；优先复用成熟组合持仓与调仓框架，不无依据扩张成通用 Alpha 平台。

## 2. 用户明确修改意见

- 01/02/03 的视觉不一致不可接受；不能通过缩小字体、压缩行高或减少留白迁就其他页面。
- 最快、最有效的界面改善是“字体放大 + 排版舒展”。
- Stitch 设计只是参考，必须查看真实运行页面，不能把预览误称为已应用。
- 原始页面的信息和业务内容不得因视觉统一丢失。
- 不要先配置 TradingView。
- 不要把旧 Parquet 缺少 sidecar 解释为数据本身格式错误。
- 必须用 Git 历史区分原作者功能与 fork 新增合同。
- 前端、数据通道等重要任务完成后要由独立审查线程循环复验。
- AlphaMaster 与其他项目必须完全隔离；不能混在一起分析、提交、打包或执行。
- 解决问题从第一性原理出发；开发用剃刀定律，验收用墨菲定律。
- 复杂任务完成后必须由独立线程按需求、逻辑、边界、代码质量、测试和实际运行六维审查；问题分阻塞项和可选优化，只强制修阻塞项。
- 对不清楚的需求一次只问一个问题，解释选项、例子和推荐理由，逐步确认到约 95% 把握。
- `reward` 是否过度依赖交易结果暂记为研究问题 `R-01`，在因果性、回测/实时一致性和模拟盘证据完成前不贸然修改。
- 网页版 GPT 等外部规划者看不到本机 Skills、memory、未提交代码、
  真实数据和运行态；其结论只负责方向、架构、依赖和反方意见。本机 Codex
  必须按真实项目 Skill、现有模块和测试重写、去重和排序，不能原样执行。
- 用户要求进攻式推进：真实关键路径一旦确定就立即开工，独立证据线并行；
  不再用泛化风险清单、重复调研或“一次只做一张最小工单”拖慢完工。只有
  不可逆操作、结果语义分叉或会改变结论的真实证据缺失才停止询问。

## 3. 已发现的主要问题

### 前端

- Stitch 预览和桌面快捷方式启动的生产页面不是同一入口，导致“修改后没有变化”的误解。
- 01、02、03 曾存在侧边栏缩进、激活背景、字号、行高、卡片边框、Tab 拉伸和图表空白带差异。
- 02 回测评估标签在轮询刷新后会被通用“回测绩效”覆盖。
- 数据卡片曾只显示“已注册”，无法区分旧数据用户登记与新版导出器验证。

### 数据与训练

- `NVDA_M5.parquet` 在 Slurm 模式报缺少 manifest；根因是 fork 新增远程身份合同，不是原始数据必然错误。
- 原作者仓库不要求 sidecar；提交 `c3717be` 才为 Slurm 增加该机制。
- 旧 MT5 数据需要可审计登记，但不得冒充新版导出器验证。
- 旧 OKX 归档与新版下载器来源语义不同；下载器必须过滤未完成 K 线。
- 数据来源 allowlist 曾在本地 loader、Slurm control、Worker、进度和训练包之间分叉。
- 训练过程曾用最小策略 JSON 覆盖完整数据身份。
- checkpoint 曾按品种扁平存放，不能安全区分周期、数据哈希和 run。

### 回测与产物

- 策略与测试数据完全同哈希的旧候选合同只能做训练集重放，阻止正常样本外评估。
- 历史策略缺少数据行数、范围和列合同，不能证明样本外边界。
- Slurm 结果曾未完整绑定 `data_start`、`data_end` 和 `columns`。
- README、新手指南和旧 GPT 交接材料曾保留已经过时的候选状态与重新训练语义。
- 旧安全 ZIP 直接基于 Git 树归档，因历史跟踪的 `training_time_XAUUSD.json` 而包含运行态；该 ZIP 未上传 GitHub，后续包必须显式排除并重新扫描。
- 原 `StackVM._normalize_output()` 的全样本时间统计会让历史时点使用未来数据，导致同一时点的前缀计算与全序列计算不一致。
- `JUMP` 类算子还存在独立因果风险：对整段序列做全局标准化会让未来数据改变历史输出，且旧策略/checkpoint 若未绑定新公式执行合同，可能被错误续用。
- 2026-07-20 用户确认后，已修复并随 `67e81f3` 推送多品种时间交集不足时的未来首报价回填：回退只做 `ffill()`，统一裁掉没有历史报价的共同前段，裁后不足 `MIN_BARS` 失败关闭；独立六维审查 `PASS`。
- `model_core/backtest.py::_turnover_quality()` 将 `tanh` 连续仓位转换为 `int(p)`；绝大多数非满仓值被截成 0，导致 reward 中的交易频率质量失真。
- walk-forward validation 分数在每个训练 step 被反复用于冠军、精英池和搜索方向，因此它属于 selection 反馈；是否存在真正独立、密封且只在策略冻结后访问的最终测试集，仍需代码级审查。
- PnL 与 IC 当前使用的目标收益索引看起来相差一期；在合成价格序列证明唯一执行时点和预测 horizon 前，只登记为待验证问题，不先改代码。

### 项目边界与移交

- 曾错误地把另一个独立项目的实盘目标写入 AlphaMaster 的上下文和网页版 GPT 总控材料。
- 该做法违反项目隔离要求：外部模型会把本机—服务器联通任务误导为无关的跨仓库规划。
- 现行要求是 AlphaMaster 移交只包含本仓库事实、需求、代码、验证和后续工单。
- 基于错误移交提交 `281db3d1` 生成的旧源码包已作废，不得交给外部模型；修正提交后必须重新打包和扫描。

## 4. 已完成修改

- 三页前端完成统一大字体、舒展排版、侧边栏、标题栏、卡片、表单、按钮和 Tab。
- 真实浏览器验证 01/02/03，无控制台、页面或 HTTP 错误。
- 新增统一数据来源合同 `data_pipeline/dataset_contracts.py`。
- 新增旧 MT5 两阶段 plan/apply 注册，绑定文件哈希、mtime、大小、来源报告与用户确认。
- MT5 导出器和 OKX 下载器自动生成 sidecar；OKX 只保留 `confirm=1`。
- 新增 A 股旧数据严格转换，不静默排序、补行或有损修复。
- checkpoint 按 `timeframe + data_sha256 + run` 隔离；`--from-scratch` 保留旧 run。
- 策略、checkpoint、Slurm run/result、发布指针和训练包贯穿完整数据身份。
- 回测可独立选择测试数据，支持 replay、out-of-sample 和 diagnostic-overlap。
- 回测评分窗口从平仓状态开始，年化依据评估数据实际覆盖范围。
- 训练包 v2 增加 allowlist、路径、数量/大小、CRC、逐文件哈希、身份一致性、暂存验证和失败回滚。
- 相关实现已在独立严格审查后提交 `d4dcb75279387c282e13b56e20e843d6801d4065`
  并推送 `main`；仓库当前按用户后续决定持续保持公开。
- `fd3e1302a77dce0900fb54513d51c3551319ef92` 已将 StackVM 最终输出改为严格因果的滚动标准化，并用公式执行合同使旧语义产物失败关闭。
- `775dad54acd0bd75e5fc75d58c424536af117e38` 已修复 JUMP 算子的因果计算，并补齐 Web 旧策略与 Slurm 混合 checkpoint 的版本拒绝。
- AlphaMaster 的 canonical 上下文、项目规则、移交说明和网页版 GPT 指令已改为单仓库、单项目目标。
- `.env.example` 已改为只描述 MT5 行情读取/数据导出，并使用明显登录占位符，避免暗示存在生产下单能力。

## 5. 尚未完成或仍需决定

- 旧 MT5 668 文件批量计划尚未 apply。
- 正式训练预算、步数、CPU、内存和 Slurm 时限尚未冻结。
- 历史策略缺少完整身份时，需要重新训练或另行迁移，不能直接宣称可做严格样本外。
- Web/API 的训练包导入入口仍固定关闭。
- TradingView 仍暂缓。
- JUMP 修复提交时的完整 unit 为 501 通过、8 个既有失败、1 项跳过，不能宣称全仓全绿；本轮文档交接不把未提交供应商候选代码的测试数字混入 GitHub 基线。
- Git 当前树仍跟踪 `training_time_XAUUSD.json`；是否删除当前文件、是否治理历史需要单独授权。
- 工程链路通过不代表策略盈利；正式研究还需要独立样本外、成本、稳健性与过拟合检验。

## 6. 当前交付原则

网页版 GPT 只读取 AlphaMaster 公开 GitHub 快照，负责方向、
架构、依赖、工单优先级和对抗审查。它们不能知道本机未提交工作区、Skills、
memory、真实数据、Web、Slurm 和测试现场，也不能直接授权执行。本机 Codex
先刷新现场，对外部工单执行 `接受 / 改写 / 删除`，再映射到真实模块与测试；
关键路径立即推进、独立证据线并行。涉及业务语义分叉或新增不可逆操作时，
再交回用户决定。

## 7. 2026-07-18 未来函数与过拟合总控交接

- 大模型供应商能力正在独立项目中先行修复；本轮 AlphaMaster 不提交供应商候选代码，避免重复劳动和跨项目污染。
- 本轮交接只打包 AM 的项目上下文、规则、用户意见、已完成的 normalization/JUMP 因果修复、尚未关闭的问题和验证证据。
- 网页版 GPT 的首要任务是从代码证明其他未来函数、时间对齐和回测过拟合风险，不能扩张到 Strategy Genome、Alpha Pool、LLM Agent、券商执行或 reward 偏好重构。
- `R-01` 继续暂缓；只有发现可复现的 reward 实现错误时，才允许把具体错误写入修复工单。

## 8. 2026-07-19 实盘方向与选择性上游对齐

### 用户确认的目标与边界

- 长期目标改为打通“训练 → 回测 → 受控全自动实盘”，市场顺序为加密货币 → 港股/美股 → A 股。
- 实盘链路的调研和后续工单只覆盖四个环节：历史/实时数据、订单执行与持仓、监控报警、风险控制。
- 成熟开源社区工具优先于自研；但工具的许可证、维护状态、市场覆盖、账户/沙盒能力和安全边界必须在实施时重新核验。
- 核心算法路线不另起炉灶。上游对齐时，量化、金融、模型训练和通用数据处理优先保持原作者逻辑；AI 大模型接入、Slurm 训练调度、前端页面和实盘接入方向必须保留。
- 上游同步永远选择性移植，不整体 merge。当前明确不移植上游的数据驱动年化估算、秒/毫秒静默修复、tqsdk 凭据/期货实现、前端重试、AI provider 微调和调试脚本。
- 用户同时明确授权将当前项目的 AI provider/前端源码、对应测试和交接文档随本次精确快照提交；真实配置、密钥、账户、原始数据、训练产物、原始日志和机器可消费的运行态文件仍不得提交，脱敏的时间戳摘要只作外部审查证据。
- 2026-07-20 用户明确授权将当前公开仓库作为临时发布目标。主交接提交 `4a897de` 已推送；该授权仅覆盖本次固定的 36 路文本快照，任何额外文件或后续发布仍需重新确认。

### 已完成并随用户授权的临时公开发布交接的三张工单

- 标准化：删除 `model_core/formula_contract.py`，将 StackVM 输出标准化与 JUMP 切换到用户选择的上游 expanding 语义，词表版本只由 token 名称派生。
- 通达信：指数走 `get_index_bars`，股票走 `get_security_bars`；增加 CST 解析、异常日期识别，并确保只删除未来未形成 K 线。
- Parquet：在文件名解析入口接受常见周期别名并归一为下游标准 token；下游合同不放宽。

### 本阶段未开始且不由本提交授权的事项

- 交易所/券商选择、真实账户、凭据、资金、仓位上限、回撤阈值、止损语义、模拟盘转真实盘门槛。
- 加密、港股/美股、A 股任一市场的订单执行、持仓同步、监控报警或风险控制实现。
- 外部调研材料中的工具“最优”结论、星标/许可证数字、数据源稳定性判断、上游提交数量、DSR 数字和过拟合结论；它们只能作为后续实时核验线索。

详细交接与事实分级见 `docs/LIVE_TRADING_DIRECTION_HANDOFF.md`。

## 9. 2026-07-23 大 A 信号优先路线

### 当前现实约束

- 用户当前没有可用于真实交易的 QMT、PTrade 或目标券商量化账户，因此无法对登录、委托、成交、撤单、持仓和交易日恢复做真实验证。
- QMT/PTrade 接口不再是当前开发主线；在真实券商渠道具备前，不用模拟接口冒充实盘闭环。
- 当前主线是“服务器 Slurm 训练 → 回测 → 普通 A 股虚拟信号 → 飞书机器人推送”；信号优先不等于停止训练与回测，三段必须边跑边贯通。

### 已确认的产品合同

- 账户语义：普通 A 股账户，只做多，不产生融券做空建议。
- 因子仓位小于 `MIN_TRADE_EXPOSURE` 时视为无信号；负因子在空仓时观望，在虚拟持仓时转为离场目标。
- 每根已收盘 K 线只产生一个确定性动作：继续持有/观望、买入、加仓、减仓、离场、止盈或止损。
- 用 SQLite 保存虚拟仓位和信号事件；同一监控项、同一已收盘 K 线在轮询或重启后不得重复生成。
- 止损与止盈按已收盘 K 线的收盘价判断。当前参数沿用 `STOP_LOSS_PCT=-2%`、`TAKE_PROFIT_PCT=+4%`；首次止盈后虚拟仓位减半。
- 调仓门槛为目标虚拟仓位变化至少 10%，避免连续仓位的细小变化产生噪声通知。
- 飞书只推送买入、加仓、减仓、离场、止盈和止损；继续持有/观望只入账、不打扰。
- 飞书遇到网络失败、HTTP 429 或服务端 5xx 时最多尝试三次；最终结果写回事件账本并显示在页面。
- 系统不连接券商、不读取交易账户、不提交订单；价格、虚拟仓位、止损和止盈均是人工执行参考。
- 因为系统不知道用户是否真实执行过历史信号，普通 A 股 T+1、停牌、涨跌停和账户实际可卖数量只能在消息中明确提示，不能伪装成已完成的券商级校验。

### 本阶段工程落点

- 通达信作为页面可选的免费 A 股行情源。
- 通达信返回的收盘时间统一换算为项目 `Bar.ts` 的开盘时间；当前只开放可确定已收盘的 1 分钟至日线，周线/月线暂不暴露。
- Windows 的 PyTorch 固定为 2.8 CPU：本机隔离实测 2.8 可导入并计算，2.12/2.13 均触发 `c10.dll` 的 WinError 1114。
- `strategy_manager/signal_lifecycle.py`：只做多动作决策。
- `web/signal_ledger.py`：虚拟仓位、事件幂等和飞书投递状态。
- `web/realtime_manager.py`：已收盘 K 线触发、动作入账和飞书投递。
- `/api/realtime/signals` 与实时分析页：显示最近可执行交易信号。
- 飞书测试与正式投递只接受官方 `https://open.feishu.cn/open-apis/bot/v2/hook/...` 地址。

## 10. 2026-07-23 服务器训练与大 A 自动流水线纠偏

### 用户纠偏后的硬约束

- AlphaMaster 的所有模型训练只能在用户服务器上通过 Slurm 提交；`TRAINING_BACKEND=local`、缺失后端或远端失败后的本机接管均直接拒绝。
- 训练、大 A 数据适配和前端不是三条可任选支线。每次大 A 训练成功后，必须继续完成回测与虚拟信号，并在前端按同一 `run_id` 展示。
- 第一轮先用短真实作业证明整条链路，再根据 `sacct` 的墙钟、累计 CPU、峰值内存和队列证据增加训练预算；不以“尚未冻结正式预算”为理由停止实际训练。

### 已落地

- `web/a_share_pipeline.py`：观察 Slurm `READY`，验证发布策略与 run/data 哈希一致，自动执行隔离 replay 回测和通达信虚拟信号。
- `pipeline_state.json`：统一记录服务器训练、成本回测和虚拟信号三段状态与证据，前端训练页直接显示。
- `web/training_manager.py`：启动构造器只接受 `TRAINING_BACKEND=slurm`；本机训练路径不再可选。
- 训练页明确显示“服务器 Slurm、单节点、实际 CPU”，不再用本机 `cpu` 文案冒充训练位置。

### 首个真实闭环

- 数据：`600519_D1.parquet`，5,955 bars，SHA-256 `4cb8912466781de76735f3bcd7873dac204098bc5a5e8647e3b8a009fd4627b8`。
- Slurm：run `run_20260723T151419Z_bdc5e5a0`、job `568306`，`cu16` 单节点，12 CPU、32 GB；20 步成功，墙钟 `00:01:25`，累计 CPU `10:09.454`，峰值内存 `1254524K`。
- 最优验证分数 `0.8649603724`，策略、checkpoint 和训练历史三类产物全部回传并完成哈希校验。
- replay 回测：累计对数收益 `2.912246`、夏普 `0.5306`、索提诺 `0.6649`、盈亏比 `2.3136`、588 笔。该结果只证明训练到回测的工程闭环，不是独立样本外收益。
- 通达信最新 500 根已收盘日线生成 `BUY`，目标虚拟仓位 `64.63%`；参考价 `1292.01`、止损 `1266.1698`、止盈 `1343.6904`。流水线验证不发送飞书，正式实时监控再按配置投递。

### 对抗审查后的硬化

- Web 启动后由后台线程持续观察 Slurm 状态，即使浏览器关闭也会推进当前 run 的回测和虚拟信号。
- 后处理不再读取可变的根目录兼容策略；只接受发布指针中逐项哈希通过、且 run/data/result manifest/产物目录全部匹配的策略。
- 通达信连接和回测超时属于可恢复故障，最多自动重试 3 次；身份、公式、报告合同和回测退出码错误立即失败，不做掩盖。
- `AlphaEngine.train()` 与 `train_file.train_from_file()` 增加统一运行时门禁，只有 Linux 计算节点上的受控 Slurm Worker 可以开始模型训练。
- 训练阶段卡片直接展示 Slurm 失败原因，并显示“等待重试”状态。
- 对抗修复后定向测试 143 通过、0 失败；完整非实盘套件 597 通过、17 失败，其中 16 个为既有陈旧测试，1 个为可单独通过的 Hypothesis 计时波动。

## 11. 2026-07-24 A50 龙头轮动基线与持续训练

### 用户新增硬要求

- AlphaMaster 最终扩展到多标的龙头轮动；先筛选 50 只近一年有轮动表现的龙头并批量训练。
- 训练不能因为开发、审查、调研或用户新指令被中断；服务器 Slurm 训练、状态监控、本地修改和对抗审查并行推进。
- 最终验收目标是 50 只每一只扣除交易成本后的样本外 Sharpe 都大于 1；不得用训练集、反复查看的验证集或同数据 replay 冒充。
- 跨标的智能调控需要调研成熟框架和真实失败案例，但架构规划由仓库推送后的网页版 GPT 负责；本机 Codex 提供真实代码、运行证据和调研证据包。
- `xiaojinlucky/AlphaMaster` 必须持续保持公开，目的是让网页版 GPT 直接读取；不得再把改为私有作为发布前提。公开不允许上传任何凭据、原始数据、数据库、模型、checkpoint、训练历史、日志或运行态。

### 当前工程落点

- 冻结 2026-07-23 中证 A50 官方成分作为 50 只龙头工程基线，并明确它不是最终“近一年轮动表现”筛选结果。
- 下载并冻结 50/50 只新浪后复权日线，物理切成训练段和统一一年封存段；封存段在 50 个策略冻结前不能揭示。
- 增加持久 SQLite 批队列、确定性 `planned_run_id`、同 run/job 恢复和单项串行推进；测试队列与真实队列物理隔离。批次请求同时冻结训练源码 SHA-256，源码漂移时不再继续认领下一项。
- `batch_a50_26e3472c68ca5c04` 已启动。第一项 `000333` / run `run_20260723T235959Z_908fc2fc` / job `568571` 已完成服务器训练、含成本 replay 和虚拟信号；训练分数 `1.2064923048`，同数据 replay 夏普 `0.7105`，不能计入最终门槛。
- 第二项 `000617` / run `run_20260723T235959Z_f4a3f24c` / job `568740` 已完成，训练分数 `1.2361105680`，同数据含成本 replay 夏普 `0.6114`，最新虚拟动作 `BUY`、目标仓位 `32.57%`。
- 第三项 `000725` / run `run_20260723T235959Z_2d4aa4b4` / job `568756` 已完成：墙钟 `00:11:21`、累计 CPU `01:30:54`、峰值内存 `1280816K`，训练分数 `1.4638690948`；同数据含成本 replay 夏普 `1.0163`，最新虚拟动作 `BUY`、目标仓位 `40.19%`。该结果仍不计入最终样本外门槛。
- 第四项 `000792` / run `run_20260723T235959Z_8bf9a46f` / job `568779` 已在 `cu06` 单节点完成：墙钟 `00:08:57`、累计 CPU `01:06:16`、峰值内存 `1264620K`，训练分数 `1.9909664392`；同数据含成本 replay 夏普 `0.9750`、累计对数收益 `3.704436`、287 笔，最新虚拟动作 `BUY`、目标仓位 `21.65%`。该结果仍不计入最终样本外门槛。
- 第五项 `000988` / run `run_20260723T235959Z_350644f9` / job `568800` 已提交；最新 Slurm 直查为 `PENDING (Priority)`，页面与队列为 `4/50`、45 待训练、1 活动、0 需处理，作业没有被取消或中断。
- Web 状态读取改为非阻塞快照；训练状态、日志、步数和流水线按同一 run 绑定，终态补拉日志，读取失败不降级为假空闲，活动批次不再被旧的 `NEEDS_ATTENTION` 批次遮挡。停止请求必须同时携带并匹配预期 run/job，旧页面请求返回冲突而不能取消新作业。
- 修复首项训练完成和独立对抗审查暴露的控制层问题：永久后处理失败不能被训练 `READY` 或历史重试状态重新启动；批次在真正提交前再次核对冻结源码；损坏或身份不明的本地 current/state 失败关闭为 `RECOVERY_UNKNOWN`；哈希验证后的不可变策略允许省略冗余 `run_id`，但显式错误 `run_id` 仍失败。现役作业没有重训、重提或取消。
- 本轮关键控制链与封存回归 `230/0`，封存与回测合同定向回归 `74/0`；完整非实盘回归 `736/16`，16 项为既有陈旧合同或测试时钟问题。热更新后已按 1920×1080 完成复验，页面显示 `4/50`、当前 `000988`、Slurm `568800`、45 待训练/1 活动/0 后处理/4 完成/0 需处理，浏览器控制台 `0` 错误、`0` 警告。
- 轮动证据、Qlib Recorder/TopkDropout 复用候选、AKShare 社区问题、Deflated Sharpe Ratio 与 Probability of Backtest Overfitting 边界整理在 `docs/A50_ROTATION_RESEARCH_EVIDENCE.md`。
- 封存评估 v2 已把扣费口径升级为机器合同：手续费、滑点和实际 `cost_rate` 必须同时出现在合同与逐只报告中，零总成本直接拒绝；全局揭盲锁只按 50 份密封文件 SHA-256 的排序多重集合生成，股票标签、评分窗口、合同路径、结果路径和策略都不能让同一批物理字节再次揭盲。封存与回测合同定向测试 `74/0`。

### 尚未完成

- 尚未按严格、可复现的“近一年轮动表现”定义重新筛出最终 50 只；当前中证 A50 只是基线池。
- 尚未完成小红书、抖音、微信公众号、雪球、知乎等社区的开发者/使用者/评估者证据覆盖。
- 尚未确定跨标的智能调控最终采用相对强弱、Top-K Drop、市场状态切换、组合优化或其他机制。
- 当前批次只完成 `4/50`，第五项已经提交并在 Slurm 等待优先级；最终封存窗口尚未揭示，没有任何 Sharpe > 1 的最终通过声明。

## 12. 2026-07-24 多标的组合控制层 v1

### 用户确认的推进原则

- 仓库持续公开是用户主动确定的协作条件，网页版 GPT 可以直接读取公开 `main`；后续提示词必须明确这一点，不得再次建议改为私有。
- 服务器训练与本地开发并行推进。实现组合层、跑测试和做对抗审查时，不得停止、取消或重启现役 Slurm 训练。
- 首版先完成一个可审计的确定性组合基线，再比较智能体、强化学习、神经网络和遗传算法；复杂算法必须证明解决了现有具体缺口。
- 开发阶段按奥卡姆剃刀保持最小，验收阶段按墨菲定律覆盖身份伪造、时间错位、跨进程并发、崩溃重试和状态篡改。

### 已落地

- 新增 `portfolio_manager`：冻结股票池合同、逐日固定窗口校准、Top-K +
  Dropout 组合选择、严格流水线适配器和 SQLite 组合决策账本。
- 中证 A50 组合决策必须绑定经过复核的 50 股合同；缺 1 只、混入池外股票、
  混行情来源、混周期、混交易日或时间倒退都会失败。
- 大 A 日线信号改为新浪后复权同源链：取 752 根已收盘日线，用最后 500 根
  计算当前信号，并为前 252 个历史时点分别重放各自 500 根窗口。
- 单股流水线产物升级为 `alphamaster_signal_simulation_v3`；在任何账本
  副作用前冻结完整信号输入，崩溃重试只能复用同一市场快照。
- 组合正式入口还会只读复核 `signal_simulation.sqlite3`；修改 JSON
  生命周期动作并同步状态哈希，仍会因与权威事件账本不一致而失败。
- 模型信号新增完整精度字段；A 股生命周期、实时管理器、SQLite 账本和
  组合适配器只用 `position_raw`、`strength_raw`、`factor_value_raw`
  做判断，四舍五入后的展示字段不能把 `0.04996` 误触发为买入。
- A50 正式入口独立强制 50 只信号全部来自 `1d` 和
  `akshare_sina_hfq_ohlcv`；组合账本读回时严格检查数值类型、重算
  `decision_id`，并绑定 SQLite 主键和 `bar_ts`。
- A 股后处理增加跨进程唯一租约；pytest 在导入全局 Web 管理器前同时隔离
  `local_runs` 和训练队列，不能触碰真实 Slurm 当前任务。
- `000333`、`000617`、`000725`、`000792` 的真实新浪 752 根日线与冻结
  全量 Parquet 的时间、float32 OHLC 和成交量逐项完全一致。
- `000333` 的真实已训练公式完成 252 点滚动校准；组合、流水线、行情、
  账本和控制链定向回归为 `226/0`，完整非实盘回归为 `816` 通过、
  `16` 失败，本轮新增测试全部通过。
  16 项仍是仓库原有的旧特征、旧算子、旧 Runner 和固定时钟合同。
- 三路独立对抗审查均已 PASS；最后一路复打 8 条精度、A50 来源/周期
  和账本篡改攻击，`8/8` 均被拒绝，复审未调用 SSH/Slurm、未触碰真实队列。
- 当时生成了 `docs/BIOMNI_PORTFOLIO_REVIEW_PROMPT.md`；该文件已于
  2026-07-26 退役为不可执行历史证据，不再发送或等待。

### 明确边界

- 旧的四股组合决策 `AM-PORT-71865D22516828A5B4FD8423` 只是一条诊断
  记录，不满足当前 50 股合同，不能作为 A50 完整组合或虚拟执行输入。
- 当时的组合控制层只生成目标权重；后续已补确定性执行内核。当时把生产账本
  和回测共用入口列为“下一张工单”的排序现已失效：当前先并行推进
  `OPS-01` 运行对账与 `RND-01` PIT 合同/覆盖矩阵，生产账本放在动态股票池
  replay 和历史时点成交状态之后，并复用现有账本与执行器。
- 当时仍只有 `4/50` 个单股 run 完成，不能生成完整 A50 组合决策；训练内
  分数和同数据 replay 仍不计入最终样本外 Sharpe 门槛。

## 13. 2026-07-24 运行提交冻结与大 A 数据源复用

### 用户确认

- 腾讯、通达信、东方财富、同花顺和 AKShare 都可以进入候选范围；
- 优先复用 GitHub 上成熟、稳定、可审查的开源项目，不自行重复实现网页接口；
- AKShare 不是禁用项，问题应通过锁版本、明确上游接口和数据身份解决；
- Longbridge 可稳定提供 A 股实时数据，历史数据源继续比较后决定。

### 训练中断根因与修复

- 原 A50 批次冻结了训练源码 SHA-256，但没有冻结服务器运行时的 Git 提交。
  本机控制层提交更新后，新的 run manifest 写入本机 `74c5cb43...`，共享远端
  运行时仍停在 `1d441774...`；Worker 因提交不匹配而正确失败。
- 失败在两个计算节点上均以极低资源占用快速复现，因此排除数据、模型和单节点
  故障。读取远端 `result_manifest.json` 后确认唯一根因是 Git 提交身份错位。
- 批队列、SQLite 迁移、请求幂等哈希、API、planned run 恢复和 Slurm manifest
  现已共同冻结 `runtime_git_commit`。批次缺少该字段时失败关闭，不再猜测。
- 代码定向回归 `104/0`，相关 Web 安全、API 与流水线回归 `49/0`；本轮没有改变 30 个
  冻结训练源文件，训练源码 SHA-256 保持
  `02350ba4e3c696461f2be9f042cb00912568442b3f9986151a79621f571eeb8d`。
- 恢复批次 `batch_8e173c5a667a4b31bd8573c760842b70` 承接剩余 45 只。
  首项 `002027` / run `run_20260724T053000Z_778a85a1` / job `569577`
  已在 `cu16` 完成 200/200 步，退出码 `0:0`、墙钟 `00:07:55`、
  累计 CPU `01:23:00`、峰值内存 `1280248K`，训练内最优分数 `1.4453`；
  同数据含成本 replay 夏普 `0.9952`，虚拟动作 `BUY`、目标仓位
  `79.61%`，整条流水线已 `READY`。第二项 `002230` / Slurm `569590`
  也已完成：训练内最优分数 `1.4389`，同数据含成本 replay 夏普
  `1.0969`，虚拟动作 `BUY`、目标仓位 `57.70%`，整条流水线
  `READY`。第三项 `002371` / Slurm `569594` 也已完成：训练内最优
  分数 `1.3664`，同数据含成本 replay 夏普 `1.0018`，虚拟动作
  `BUY`、目标仓位 `32.40%`，整条流水线 `READY`；下一只 `002475` /
  Slurm `569604` 已在 `cu20` 开始训练。

### 数据清单与选型

- A50 当前正式数据为 50/50 只 AKShare 新浪后复权日线：全量 119,744 行、
  训练切片 107,665 行、封存切片 24,679 行；另有 50/50 只新浪前复权对照。
- 旧大 A 库含 4,273 只个股、17,091 个个股 Parquet 和 215 个指数的
  860 个 Parquet。个股四周期总计 156,736,193 行；4,143 只在日线、
  60 分钟、15 分钟和 5 分钟四个周期都达到约两年最低行数。
- 旧库在数量上足够做大范围回测，但缺少逐文件来源、复权方式、成交量单位和
  上游响应哈希，只能作为 `ashare_local` 的隔离探索数据，不能冒充当前 A50
  正式训练数据。
- 正式选型冻结为：AKShare 新浪后复权继续做当前十年历史训练主源；
  Longbridge 官方 SDK 做实时主源和历史交叉核验源；AKShare 继续按新浪、
  腾讯、东财具体接口建立独立身份；easyquotation
  只做腾讯/新浪实时交叉核验；efinance 不冒充独立东财冗余；pytdx/mootdx
  只做诊断；pywencai/thsdk 只做股票池和元数据。
- Longbridge 对冻结 A50 的 50/50 只最新日线页请求成功，单次各返回
  1,000 根且可按日期向前分页；价格为前复权，成交量单位为手。抽样最新
  收盘价与 AKShare 前复权一致，但 600519 的 2016 年前复权价格为负。
  因而 Longbridge 数据必须建立独立合同和新模型，不能送入当前新浪后复权模型。
- 完整证据、真实接口结果、社区项目和接入门槛见
  `docs/A_SHARE_DATA_SOURCE_SELECTION.md`。

## 14. 2026-07-25 外部总控与本机执行重新对齐

- `.agents/skills` 与 `.claude/skills` 各有 29 个同名、可解析的项目 Skill，
  但都被排除在 Git 快照之外；外部模型只能从公开文档看到部分名称，看不到
  真实 Junction、内容和实时安装状态，也不能调用。AlphaMaster 不是
  OpenXQuant workspace，因此本机只借用这些 Skill 的量化语义和审计门，
  再映射到 AlphaMaster 的现有代码，禁止照搬 OpenXQuant CLI 命令或自动
  初始化 workspace。
- 旧 `docs/LOCAL_EXECUTION_CONTEXT.md` 列出的 `github:github` 和
  `github:yeet` 当前不可调用，已从执行依赖中删除。发布仍由本机 Git 实时
  检查和 `meta-ship` 适配，并服从本项目精确文件确认规则。
- 网页版 GPT 对提交 `b3bbefef6aa14ca314a3f3db6ad8babb9d595ea2`
  的 FreeStockDB D1 / Slurm sidecar 目标裁决为 `PASS`，同时确认系统未完工。
  后续不得重建 DatasetManifest、sidecar 上传/finalize、Worker 物化或严格
  loader；只验证真实成功、失败、恢复和 published 路径是否断链。
- 当前拆成两条互不等待的首单：运营线先对账已完成的 Slurm `570548`，
  收敛结果包、后处理和本地队列状态；研发线先把已有 255 个沪深300权重
  时点冻结成可执行 PIT 合同，明确何时可知、从哪个交易日生效，并生成
  949 个历史代码的精确覆盖/隔离矩阵。其后用小批历史退出现任池的股票验证
  抽取器，再扩展 649 个未导出代码，并将动态股票池接入组合 replay。
- 现有虚拟执行内核已经覆盖 T+1、停牌/涨跌停输入、整手、合法零股卖出、
  先卖后买、现金、佣金、印花税、过户费和滑点。A 股执行工单改为时点状态
  数据、生产账本、恢复链和端到端标签的缺口审计；PIT 优先扩展现有
  `portfolio_manager/universe.py::UniverseContract`，账本优先复用
  `portfolio_manager/ledger.py` 与 `web/signal_ledger.py`，不从零重造
  股票池合同、第三套 SQLite 账本或撮合器。
- 历史 `GPT5_6SOL_HANDOFF`、`GPT_WEB_PRO_EXTENDED_TASK`、
  `GPT56_PRO_EXTENDED_*` 和 `LIVE_TRADING_DIRECTION_HANDOFF` 已统一标成
  不可执行历史证据。旧的 GitHub MCP 硬依赖、默认私有仓库、单一最小工单
  和重复审批规则不得再进入当前计划。

## 15. 2026-07-26 WEB-only 总控与持续发布授权

### 用户确认

- 当前 goal 只使用网页版 GPT 做外部审核，不再把 BioMNI 作为第二总控、
  复审门或等待条件；BioMNI 提示词退役为不可执行历史证据。
- `xiaojinlucky/AlphaMaster` 始终保持公开，选择方案 A；不得再把改为私有
  作为发布前提。
- 用户对当前 goal 内经过精确暂存、安全扫描和实时远程核验的提交与推送给出
  持续授权，不再逐次重复询问。清单外文件、删除、重命名、密钥或其他安全
  阻断仍须停止，不能利用持续授权混入。

### 当前执行口径

- 唯一外部入口为 `docs/WEB_GPT_CONTROLLER_PROMPT.md`。网页版 GPT 只给
  方向、依赖、反例和工单；本机 Codex 按真实 Skills、memory、代码、数据、
  Slurm 和未提交工作区执行 `接受 / 改写 / 合并 / 删除`。
- 当前公开 `main` 推送完成后，直接由网页版 GPT 审核最新固定提交；不再生成、
  发送或等待 BioMNI 总控结果。
