# CONTEXT

## 当前目标

当前以普通大 A 账户为第一落地方向。用户没有可验证的 QMT/PTrade/目标券商实盘渠道，因此本阶段不接券商、不发单；先贯通“服务器 Slurm 模型训练 → 成本回测 → 通达信已收盘 K 线虚拟信号 → 可选飞书推送”。Windows 本机只负责数据准备、控制、回测、信号与前端，所有模型训练一律提交服务器 Slurm，禁止本机训练。

当前提交不接入真实账户、不发单、不保存交易凭据。大 A 语义固定为普通账户只做多；负因子在空仓时观望，在虚拟持仓时转为减仓或离场目标。网页版 GPT 只读取 AlphaMaster 的已提交 GitHub 快照，充当总控和工单设计者；本机 Codex 才能在真实环境中复核并执行。

## 2026-07-24 A50 龙头基线与持续训练

### 2026-07-25 评分时间合同修复候选

- 已把目标收益统一为
  `target_ret[t] = log(open[t+2] / open[t+1])`，因子、仓位和收益按同索引
  `t` 配对；训练评分、回测、成本、换手、相关性和有效性评估统一裁掉原始
  序列末尾两根不可实现收益。
- 新的 score-bearing 产物必须携带
  `open_t1_t2_same_index_tail2_v1` 评分合同。合同已贯穿 Slurm run/result
  manifest、worker、checkpoint、策略、训练历史、发布指针、训练包、Web
  展示/导出、回测和实时信号；旧合同产物只能保留作诊断，不能进入新合同
  排名或生产信号。
- 为不打断现役旧批次，本轮修复已迁入 `codex/p0-scoring-contract` 隔离
  分支和独立 worktree；现役 `main` 工作目录恢复为干净的
  `99ea362281a606fa2a9885d21d7a4d16b2c56542`，训练源码 SHA-256 重新精确
  匹配冻结值
  `02350ba4e3c696461f2be9f042cb00912568442b3f9986151a79621f571eeb8d`。
  没有停止、取消、重提或重启 Web/Slurm。新合同代码在受控合并和重启前
  不会进入现役进程。
- 当前 24 个受影响测试文件合计 413 项通过、0 项失败；完整 unit 为
  920 项通过、7 项失败。7 项均为本轮修改前已存在的陈旧算子/词表数量、
  20/65 特征维度断言和固定历史墙钟测试，本轮新增链路没有失败。
- 生产扫描与 Runner 现在绑定同一有序多公式集合、公式集合 SHA-256、信号
  阈值、固定手数、成本率、年化频率和“方向不变时保持入场尺寸”状态机。
  缺扫描、空扫描、策略被替换、非法 token、部分公式/品种失败、品种路径
  穿越、持仓读取失败和未知入场 exposure 均失败关闭；dry-run 在统一交易
  边界只放行明确只读方法，未知调用默认拦截。
- 正式 MT5 数据只请求 `start_pos=1` 的已收盘 K 线；缓存允许同时间戳最终
  OHLC 覆盖，并用 `mt5_closed_bars_pos1_v1` sidecar 隔离旧缓存：在线旧
  合同强制全量重建，离线拒绝。启动和循环调仓都要求完整品种、正 int64、
  严格向前的已收盘时间戳。
- 时间链与墨菲链两路独立 Agent 最终复验均为 PASS。没有连接、停止、重提
  或重启 Web/Slurm；现役 `main` 仍保持冻结源码。

### 2026-07-24 大 A 量化生态调研与对抗审查

- 已完成一轮“先广泛、再深入”的大 A 量化模型训练与回测生态调研，正式报告见 `docs/A_SHARE_QUANT_ECOSYSTEM_RESEARCH_20260724.md`。覆盖 GitHub 源码/测试/Issue、Reddit、B站、微信公开文章、QMT/VeighNa/聚宽/BigQuant 社区和商业平台官方资料；小红书、抖音完整正文与评论区仍受登录阻断，已明确列为补证，不能声称已读。
- 三路独立 Agent 分别从开发者、使用者和评估者角度完成反方审查。结论是不引入第二套生产主链：保留 AlphaMaster 现有 Slurm 身份链、密封评估和 A 股虚拟执行；AKQuant/RQAlpha 只作隔离差分与黄金测试，Qlib/vn.py alpha 只吸收研究层，QuantStats/Alphalens/skfolio 分别限定在展示、因子诊断和组合基准。
- 当前 P0 是历史时点股票池、时点有效的 ST/板块/涨跌停/停牌状态、信号—标签—成交时钟、目标收益末尾补零裁剪、因子有效性评分的 horizon、selection 与 sealed test 分离，以及训练代理回测和最终 A 股执行回放的明确分层。P1 是公司行动、时点费用、多交易日停牌订单、恢复等价性、全局试验账本和报告差分。
- 用户明确系统只用于个人研究，非商业限制不作为候选降级理由；但用户授权不能替代许可证。MIT/Apache/BSD 代码需保留通知，无许可证项目只阅读、观察行为或独立实现，不复制或改写原代码。
- 首轮只读审查和文档整理没有停止、重提、取消或修改现役 Slurm 作业，也没有改动训练、回测和执行代码。
- 后续隔离实验已落地到 `experiments/a_share_execution_diff/` 和 `experiments/a_share_research_layer_diff/`，完整结果见 `docs/A_SHARE_QUANT_EXPERIMENTS_20260724.md`。所有第三方源码、虚拟环境和运行结果只在 `scratch/`，没有进入生产依赖。本轮实验命令和代码路径没有调用 SSH、Slurm 或 Web API；现役作业没有被取消、重提或覆盖。
- 固定一手费用样本中，AlphaMaster 与手算、AKQuant 完全一致；RQAlpha 默认股票费用实现对齐佣金倍率后仍少 0.02 元过户费。研究层运行样本只确认 AlphaMaster 的目标收益构造函数和 vn.py 正常历史区间，同时复现 vn.py 的重叠重复、空字典绕过和全空报错；Qlib 只完成发布包与最新快照的源码合同检查，不是完整运行证据。
- 隔离反例与源码审查发现四处 P0：`AlphaEngine._compute_ic` 与 `MT5Backtest._ts_ic_stability` 都额外右移一根；IC、PnL 和换手成本路径没有裁掉目标收益末尾两个补零；`model_core.evaluator.score_all(..., horizon=1)` 与 `EffectivenessEvaluator(target_horizon=1)` 都默认只裁 1 根，`prune_features.py` 调用模块级 `score_all` 时也未显式传 2。上述问题已在 2026-07-25 的隔离修复候选中改正；现役 Slurm 批次仍使用冻结的旧源码，只能保留为旧语义诊断基线，新旧结果禁止混排。

### 2026-07-25 free-stockdb 可用性调研

- 网页版 GPT 已完成 `hello245m/free-stockdb` 深度审计，报告保留在 `https://chatgpt.com/c/6a640657-cd78-83ee-a985-58f551cbaa81`。用户随后明确决定：系统仅供个人研究，数据来源不透明和再分发授权不作为放弃使用的理由；后续按“先取得全量数据，再做内部质量门禁”的路线推进。
- 作者当前公开口径是约 5GB 日线、约 20GB 含全量分钟线；首次同步按 15–25GB、同步峰值按 25–35GB、同时保留原库与 AlphaMaster Parquet 按 50–80GB 规划。新增的财务、期货、指数、因子、Tick 和实时接口属于在线补充查询路径，不是这 20GB 本地库；作者在 Issue #40 说明全市场 Tick 约 10TB，当前接口不是批量下载接口。
- 本机 `E:/F:/G:` 分别约有 383GB、474GB、1.58TB 可用，20GB 本地库无需为了容量放服务器；服务器 `/hwdata` 约有 3.1PB 共享可用空间、计算节点本地 `/tmp` 约有 1.3TB。考虑官方发行包和更新器当前以 Windows 为主，最短路线是先在 Windows 独立数据目录同步，再转换并冻结 AlphaMaster 所需 Parquet，最后只把训练数据上传服务器。
- 工程风险仍保留为接入门禁：Release 中未签名的 `exe/pyd` 与公开源码无法建立可复现同源关系；Python 前复权因子加载异常会被静默吞掉并可能返回未复权数据；HTTP 镜像、未落地的 LAN 鉴权、Windows 测试态发行和停服务更新不能被包装成已解决。
- 网页报告把“GitHub 页面为 MIT、raw LICENSE 为 Personal Free License”列为 P0，但本机冻结 HEAD `66dd0b2a801b77998dbcde5bf5ccd0000c1e5bd5`、该提交的 raw URL 和 `main` raw URL 在 2026-07-25 复核均为 MIT；该条属于网页版错误，不能进入正式证据链，也不影响其余工程风险的检查。
- 已在用户授权的 G 盘独立目录 `G:\QuantData\free-stockdb` 执行 Release
  `v0.2.1-more-power` 全量同步。最终服务器清单生成于
  `2026-07-25 10:32:02 +08:00`，数据目录为
  `G:\QuantData\free-stockdb\package\stockdb\data`：共
  `10,181,938,798` 字节（`9.483 GiB`）、293 个 `.ldb` 分片、0 个
  `.part`。清单中的 296 个实际落盘文件全部通过 SHA-256 校验，0 个哈希
  不一致；`LOCK`、`LOG`、`LOG.old` 是数据库启动时生成的运行文件，不是
  行情缺失。
- 本地真实查询盘点为 7,563 个可查询代码：日线 18,465,538 条，
  7,563/7,563 至少有 1 条，整库范围 `2000-01-04` 至 `2026-07-24`；
  1 分钟线 270,210,217 条，7,171/7,563 至少有 1 条，整库范围
  `2025-01-02 09:31`
  至 `2026-07-24 15:00`。整库最早日 `2025-01-02` 的 999 个首条记录
  全部落在按前缀粗分的 `1/5` 组；
  沪深股票代码组 `0/3/6` 的最早分钟记录为 `2026-01-21 09:30`，
  其中 5,104 个代码的首条记录落在该日，其余有数据代码首条记录更晚；
  该组覆盖 5,198/5,201，缺 `000022`、`000043`、`600849`。北交所
  `920xxx` 的 309 个代码当前没有分钟线。7,171 个有分钟线的代码中，
  7,170 个最后交易日为 `2026-07-24`，另 1 个为 `2026-07-17`。
  上述起止键只证明记录边界，尚未证明区间内逐日、逐分钟无缺口。按代码
  前缀粗分，`1/5` 组共有 2,053 个基金、ETF 等代码；证券身份尚未用独立
  主数据表复核，因此不能把 7,563 说成 7,563 家 A 股公司。
- `stockdb.exe` 的默认可写启动会触发 LevelDB 分片整理；验证后已经关闭
  数据库并重跑更新器，把目录恢复为服务器清单快照。更新器、监控器和
  数据库进程均已退出。后续接入 AlphaMaster 前先设计只读查询副本与
  Parquet 冻结合同，不能直接让训练读取这份可变 LevelDB。分钟回测还要
  独立确认 `09:30/09:31/15:00` 时间戳表示分钟开始还是结束、是否包含
  集合竞价；本轮只证明可读和数量边界，尚未完成时间语义审计。

### 2026-07-25 free-stockdb 沪深300 AM 输入冻结

- AM 当前 65 个原生特征和 62 个已注册算子只直接读取
  `open/high/low/close/volume`；训练目标固定为
  `log(open[t+2] / open[t+1])`。原生 A 股周期为 `M5/M15/H1/D1`，
  当前最小长度分别是 `23232/7744/1936/484` 根。
- v1 探索数据保留在
  `G:\QuantData\free-stockdb\am_exports\20260724_csi300_am_inputs_v1`：
  M1 300 只、8,767,970 行，范围 `2026-01-21 09:30` 至
  `2026-07-24 15:00`；M5/M15/H1 各 299 只，均达不到 AM 当前最小
  训练长度。`600958` 的 M1 含 1,446 条非法 OHLC，`688981` 主要是
  5 分钟粒度却被标成 M1；分钟时间标签语义也未获上游独立确认。因此
  v1 分钟数据只作原始证据，禁止直接进入正式训练。
- 工程可训练的 v2 D1 数据位于
  `G:\QuantData\free-stockdb\am_exports\20260724_csi300_am_inputs_v2`。
  300 只共 1,180,991 行，其中 273 只通过严格来源合同、15:00 收盘
  时间、OHLCV、最小长度、文件哈希和前复权连续性检查；27 只进入
  `D1_quarantine`，没有插补或静默修复。阻断原因是：3 只历史不足、
  4 只缺前复权因子、20 只前复权连续性异常。当前连续性阈值是
  10 个百分点；273 只通过者中仍有 12 只最大差异超过 5 个百分点，
  因此这里只能用于单股探索性训练和工程验证，不能称为独立权威源核验
  通过的正式沪深300数据。
- v2 的每只可训练标的都有 sidecar manifest，冻结源数据库快照哈希、
  抽取脚本哈希、版本、复权方式和因子点数；项目新增独立来源
  `ashare_free_stockdb_qfq`，本地检查和 Slurm 训练入口都拒绝无 manifest
  文件。sidecar 会以固定文件名、哈希、大小和完整来源身份随 Parquet
  上传，并在 Worker 物化到数据旁再由同一严格 loader 复核。最终验收为
  300 个数据文件通过、273 个工程可训练、27 个隔离；相关测试 136 通过、
  0 失败，排除 4 个已知陈旧测试文件后的单元回归 841 通过、0 失败。
- 当前 300 个代码是 free-stockdb
  `get_index_stocks("000300.XSHG", date="2026-07-24")` 返回的现任
  沪深300成分候选，尚未使用中证指数官方文件独立复核。历史回测必须按
  已下载的 255 个月度权重/成分时点重建股票池，不能把现任成分回填到过去。
  当前历史权重涉及 949 个代码，v2 尚缺 649 个历史成分；正式历史沪深300
  组合回测继续阻断。原生公式训练只需 OHLCV；正式 A 股成交回放还需要
  时点有效的停牌、
  涨跌停、ST、交易日历、费用、整手和账户状态。
- free-stockdb 在线接口实测返回 `测试期超2000次`。只能确认试用期累计
  调用超过 2,000 次后被阻断，官方页面未说明每日重置、永久免费额度或
  一分钟线独立额度；不得将其解释为“每天 2,000 次”。作者 2026-07-23
  发布的新全量接口要求至少使用当日版本，并明确在线接口只作全量数据
  补充查询、不要批量扫描。当前
  `G:\QuantData\free-stockdb\free-stockdb-windows-v0.2.1-more-power.zip`
  为 3,910,405 字节，SHA-256 是
  `07e6b2eefd92e9c1a7cf2ecfb3fc80ea87ad97e4a4dce58d8486415100ab03fd`，
  与作者当前 GitHub Latest Release 资产逐字节一致，无需再次替换；未来
  升级仍必须使用独立 staging 目录，不能覆盖当前源库或查询副本。

### 2026-07-24 最新续接状态

- 本机 Web 控制进程已恢复运行。`squeue` 对离开活动队列的旧 job 返回
  `Invalid job id` 时，控制层会读取并严格复核同一 run/job 的结果 manifest；
  manifest 在共享文件系统上短暂不可见时保持 `DOWNLOADING` 并重试，不会误写
  `FAILED` 或重复提交。
- 恢复批次 `batch_8e173c5a667a4b31bd8573c760842b70` 当前为唯一活动批次：
  6 项 `READY`，`002625` / Slurm `570036` 因优先级排队，38 项继续等待。
  正式训练没有被基准、测试或代码审查取消、重启或重复提交。
- A 股虚拟执行内核已实现：目标权重不重选，显式停牌/涨跌停状态，T+1，
  买入整手、合法零股卖出，先卖后买，现金、佣金、最低佣金、印花税、
  过户费和滑点约束。组合决策同时绑定现金、逐批持仓和决策日估值行情；
  篡改决策、账户或行情会在生成订单前失败。当前尚未写入生产 SQLite
  执行账本，也未接飞书或券商。
- 服务器 12 核、同一 192 条公式、同一 5 折 walk-forward 的隔离基准保留
  生产顺序依赖：重计算可并行，但相关性惩罚和冠军因子池仍按公式顺序更新。
  五轮平衡结果中，2×6 线程仅为串行的 `1.025x`，4×3、6×2、12×1 均更慢；
  所有分数与状态摘要差异为 0。因此当前不改生产评估循环。
- 本轮相关回归为 `196/0`；冻结训练源码 SHA-256 仍为
  `02350ba4e3c696461f2be9f042cb00912568442b3f9986151a79621f571eeb8d`。

- 当前先用 2026-07-23 冻结的中证 A50 官方成分打通 50 只串行工程链。它是高流动性、跨行业龙头候选池，不是“近一年轮动表现最强 50 只”的最终筛选结果；证据与缺口见 `docs/A50_ROTATION_RESEARCH_EVIDENCE.md`。
- 50/50 只新浪后复权日线已下载并冻结；训练数据统一截止 2025-07-23，2025-07-24 至 2026-07-23 作为物理隔离的最终封存窗口。成分合同 SHA-256 为 `987387945fba0cb778b648860bc7579a3cf49e9c3b788596464f714e968bb896`，切分合同 SHA-256 为 `ec5a9549587802b3f0f82fd566d953315b3ecbf0a42d24f21795a8978a60cf73`。
- 持久队列 `batch_a50_26e3472c68ca5c04` 已启动，共 50 项、每项 200 步、单节点 12 CPU、32 GB、30 分钟；批次冻结训练源码 SHA-256 `02350ba4e3c696461f2be9f042cb00912568442b3f9986151a79621f571eeb8d`。第一项 `000333` / run `run_20260723T235959Z_908fc2fc` / job `568571` 已完成服务器训练、成本 replay 和虚拟信号，队列状态为 `READY`。训练分数 `1.2064923048`，同数据含成本 replay 夏普 `0.7105`，后者不是样本外成绩。
- 第二项 `000617` / run `run_20260723T235959Z_f4a3f24c` / job `568740` 已完成；单节点墙钟 `00:11:27`，训练分数 `1.2361105680`，同数据含成本 replay 夏普 `0.6114`，最新虚拟动作 `BUY`、目标仓位 `32.57%`。第三项 `000725` / run `run_20260723T235959Z_2d4aa4b4` / job `568756` 也已完成；单节点墙钟 `00:11:21`、累计 CPU `01:30:54`、峰值内存 `1280816K`，训练分数 `1.4638690948`，同数据含成本 replay 夏普 `1.0163`，最新虚拟动作 `BUY`、目标仓位 `40.19%`。这仍不是样本外成绩。
- 第四项 `000792` / run `run_20260723T235959Z_8bf9a46f` / Slurm job `568779` 已在 `cu06` 单节点完成；墙钟 `00:08:57`、累计 CPU `01:06:16`、峰值内存 `1264620K`，训练分数 `1.9909664392`，同数据含成本 replay 夏普 `0.9750`、累计对数收益 `3.704436`、287 笔，最新虚拟动作 `BUY`、目标仓位 `21.65%`。第五项 `000988` / run `run_20260723T235959Z_350644f9` / job `568800` 也已完成服务器训练、成本 replay 和虚拟信号。
- 第六项 `002027` 首次派发暴露了批次只冻结训练源码 SHA、没有冻结服务器 Git 提交的问题：本机控制层已更新到 `74c5cb43...`，共享远端运行时仍为 `1d441774...`，Worker 因 manifest 的 Git 提交不匹配而正确拒绝。两个隔离节点上的恢复尝试复现同一控制层错误，证明它不是模型、数据或节点故障。
- 批队列现已同时冻结 `source_sha256` 和 `runtime_git_commit`，幂等请求、SQLite、API、planned run 恢复和 Slurm manifest 全链绑定这两个身份。定向回归 `104/0`，相关 Web 安全/API/流水线回归 `49/0`，冻结训练源码 SHA 仍为 `02350ba4e3c696461f2be9f042cb00912568442b3f9986151a79621f571eeb8d`。
- 恢复批次 `batch_8e173c5a667a4b31bd8573c760842b70` 从 `002027` 起承接剩余 45 只，服务器运行提交明确冻结为 `1d44177496a87a8d881cdc87e26af5835cdaab18`。首项 `002027` / job `569577` 已完成整条流水线：训练内最优分数 `1.4453`，同数据含成本 replay 夏普 `0.9952`，虚拟动作 `BUY`、目标仓位 `79.61%`。第二项 `002230` / job `569590` 也已完成：墙钟 `00:07:47`、累计 CPU `01:21:48`、峰值内存 `1268664K`，训练内最优分数 `1.4389`，同数据含成本 replay 夏普 `1.0969`、累计收益 `3.232361`、162 笔，虚拟动作 `BUY`、目标仓位 `57.70%`。第三项 `002371` / run `run_20260724T053000Z_5c67231d` / job `569594` 已在 `cu16` 完成：墙钟 `00:07:51`、累计 CPU `01:21:44`、峰值内存 `1268564K`，训练内最优分数 `1.3664`，同数据含成本 replay 夏普 `1.0018`、累计收益 `3.033214`、328 笔，虚拟动作 `BUY`、目标仓位 `32.40%`；三项均为 `READY`。第四项 `002475` / run `run_20260724T053000Z_feb55a3c` / job `569604` 已在 `cu20` 开始训练。
- 原批次保留前五项 `READY` 并因缺少运行提交身份停在 `NEEDS_ATTENTION`，不会继续认领后 45 只；恢复批次是唯一活动批次。此处 8/50 是当时快照，最新运行态以本节顶部“最新续接状态”为准。
- 多标的组合控制层 v1 已落地：完整冻结股票池合同 → 752 根新浪后复权同源日线 → 各历史时点独立 500 根窗口重放 → 252 点单股分位校准 → Top-K + Dropout → 等权目标与 SQLite 审计。A50 正式入口只接受同一交易日、同一 `1d` 周期、同一上海 15:00 收盘时点和同一 `akshare_sina_hfq_ohlcv` 来源；4/50 或 50 只一致使用错误周期/来源都不能冒充正式 A50。
- 单股流水线升级为 `alphamaster_signal_simulation_v3`，先冻结市场输入再写账本；崩溃重试不能换交易日。后处理增加跨进程唯一租约；组合正式入口从 READY 状态、run manifest、回测、发布策略、冻结输入和输出哈希内部生成身份，不接受调用方自报 expected。信号判断只读完整精度的 `position_raw`、`strength_raw` 和 `factor_value_raw`，四舍五入字段仅供展示；组合账本读回时重算 `decision_id` 并绑定 SQLite 主键和 K 线时间。
- Web 只读状态接口改为本机快照，不再让浏览器轮询阻塞 SSH；状态、日志和步数按同一 run 绑定，终态会补拉最后日志。前端状态读取失败时保留上次可信快照并禁用训练操作，不再误显示空闲；活动批次优先按真实 `active_item` 选择；停止操作必须同时携带并匹配预期 run/job。
- 已修复首项后处理与独立对抗审查暴露的控制层问题：不可变策略产物允许省略冗余 `run_id`，但显式错误 `run_id` 必须失败；训练已经 `READY` 时，永久后处理失败不能被历史重试状态重新启动；停止请求绑定旧 run/job 时返回冲突；批次在真正提交前再次核对冻结源码；损坏或身份不明的本地状态失败关闭为 `RECOVERY_UNKNOWN`，不能显示空闲或启动新 run。现役作业没有重训、重提或取消。
- pytest 在导入 Web 管理器前同时隔离 `local_runs` 和训练队列。四只已完成股票的真实新浪 752 根日线与冻结全量 Parquet 的时间、float32 OHLC 和成交量逐项一致；`000333` 已用真实公式完成 252 点滚动校准。组合、流水线、行情、账本与控制链定向回归 `226/0`；完整非实盘回归 `816/16`，16 项仍为既有陈旧合同，本轮新增链路没有失败。
- 多标的组合 v1 的三路独立对抗审查均已 PASS。最后一路亲自重放 8 条精度、A50 来源/周期和账本篡改攻击，`8/8` 均被拒绝，定向复验 `70/0`，且没有调用 SSH/Slurm 或触碰真实训练队列。
- 为保证训练不断，本轮没有重启现役 Web/批队列进程；组合 v1 仍是本地已验证候选，未来受控重启后才进入长驻进程。当前旧进程继续监控并推进 `000988` / Slurm `568800`。
- 封存评估审查边界：前述 5 个控制层 P1 来自已经完成的独立对抗审查并已修复；针对最后一处“同一批密封字节改标签或改窗口再次揭盲”的增量修复，两次追加子 Agent 均未在限定时间内返回结果，因此当前证据是主 Agent 手工调用顺序审查、反例测试和机器回归，不能声称该最后增量已经独立复核 PASS。
- 用户的“50 只每只扣费后样本外 Sharpe > 1”是最终整批门槛，不是允许反复查看封存集的调参规则。首轮 200 步训练和同数据 replay 只用于跑通与筛选，最终必须冻结 50 个策略后一次性揭示统一封存窗口；当前没有任何一只可被声称已经通过最终门槛。
- 封存评估合同已升级为 v2：合同和逐只报告必须同时绑定严格大于 0 的手续费、滑点和实际 `cost_rate`；报告声明的成本率必须与回测实际使用值一致。一次性揭盲锁只按“50 份密封文件 SHA-256 的排序多重集合”生成数据集身份，落在当前系统用户的全局注册目录；股票代码标签、测试窗口、合同路径、结果路径和策略 SHA 都不参与锁身份，因此同一批物理字节换标签、换窗口或换策略仍会命中同一锁。封存与回测合同定向测试 `74/0`。
- 大 A 数据源不再自造网页抓取器，选型与本机数据清单见 `docs/A_SHARE_DATA_SOURCE_SELECTION.md`。当前十年训练继续使用 AKShare 新浪后复权；Longbridge 为实时主源和历史交叉核验源。Longbridge 50/50 只最新页请求成功，单次上限 1,000 根且可向前分页；其价格为前复权、成交量为手，不能直接送入当前后复权模型。腾讯、东财、通达信和同花顺按成熟工具的真实能力使用，禁止无痕换源。

## 2026-07-19 当前方向与上游同步边界

- 用户确认采用“选择性移植”，永不整体 merge 上游：量化、金融、模型训练与通用数据处理优先保持上游逻辑；AI 大模型接入、Slurm 训练调度、前端页面和实盘接入方向必须保留。
- 本轮三张上游同步工单已在本地工作树完成：StackVM/JUMP 改为上游 expanding 标准化并删除 formula contract；通达信指数改走指数接口；Parquet 文件名入口支持常见周期别名。
- 本轮已完成本地范围测试、真实通达信请求和独立六维复验。2026-07-20 用户明确授权将当前公开仓库作为临时发布目标；36 路精确快照已随 `4a897de` 推送到 `main`，`.env`、真实配置、密钥、数据、模型和日志未上传。
- 不移植上游的数据驱动年化估算、秒/毫秒静默修复、tqsdk 凭据/期货实现、前端重试和 AI provider 微调，除非用户为相应区域另开工单。

## 稳定基线

- 目标发布仓库：`https://github.com/xiaojinlucky/AlphaMaster`，分支 `main`，上游为原作者仓库。用户已于 2026-07-24 明确要求仓库持续保持公开，以便网页版 GPT 与 BioMNI（BIOMNI）直接读取源码和证据；不得再把改为私有作为发布前提。公开不放宽精确暂存、密钥扫描和运行态排除规则。
- 本轮选择性上游同步开始前的本地与远程功能基线：`e747eadf78f95777315ea961671c23491d116a7e`；最终交接 SHA 由 GitHub `main` 动态读取，不在提交内自我硬编码。
- 前端三页统一完成于 `186217ecb7c9658187b8a335c1445af576263f02`；相关视觉合同测试 21 项通过，01/02/03 已统一大字体、舒展排版、侧边栏、卡片和控件基线。
- `093e317` 已将 `times.py` 的 Tushare 明文 token 改为只读取本机 `TUSHARE_TOKEN` 环境变量；但旧 Git 历史仍含旧值和历史训练产物，必须先轮换 token，且不得把 `.git` 交给外部模型。

## 已完成功能

- A 股旧数据严格转换与统一数据来源合同。
- 旧 MT5 两阶段可审计登记；新 MT5/OKX 自动 sidecar。
- `timeframe + data_sha256 + run` checkpoint 隔离。
- 策略、checkpoint、Slurm run/result、发布指针和训练包完整身份贯穿。
- 回测独立测试数据、replay / out-of-sample / diagnostic-overlap 和评估数据年化。
- 训练包 v2 路径、成员、大小、CRC、哈希、身份与失败回滚校验。
- 历史 200 根滚动标准化与公式执行合同的修复证据仍保留在 Git 历史和验证文档中；用户随后决定以原作者上游的 expanding 标准化为当前语义，因此运行时代码已删除 `formula_contract.py`。
- 当前 `StackVM._normalize_output()` 与 `JUMP` 使用 expanding 统计，`VOCAB_VERSION` 只由有序 token 名称派生；旧版带执行合同的产物版本不再匹配当前词表版本。
- 主线程相关回归 140 通过、0 失败；独立审查扩大回归 229 通过、0 失败；完整 unit 444 通过、8 个既有失败。
- 当前受影响公式、数据与 Web 契约测试 151 通过、0 失败；AI provider 定向测试 28 通过、0 失败。完整 unit 为 513 通过、9 个既有失败；排除本轮相关测试后为 402 通过、相同 9 个失败。
- 通达信真实请求已验证 `sh000001` 与 `600519`；`drop_forming=True` 只会移除未来未形成 K 线，不会丢掉最新已收盘 K 线。
- 普通大 A 只做多信号生命周期、SQLite 幂等虚拟仓位、飞书重试与前端信号历史已完成；动作覆盖买入、加仓、减仓、离场、止盈、止损和不打扰的持有/观望。
- `web/a_share_pipeline.py` 已把同一大 A run 的 Slurm 训练、replay 回测和通达信虚拟信号串成自动后处理；后台监控不依赖浏览器轮询，外部行情/回测超时最多重试 3 次，身份或合同错误立即失败。
- 后处理只读取 `published_training/current_<symbol>.json` 指向且逐项哈希通过的 Slurm 发布包，并同时匹配 run、数据、结果清单和产物根目录；可变的兼容策略副本不能再冒充当前训练结果。
- `AlphaEngine.train()` 和 `train_file.train_from_file()` 均要求 Linux Slurm Worker 运行标记、Slurm job ID 与计算节点；Windows 本机和普通命令行会在读取训练数据前失败。
- 首个真实闭环：`600519_D1.parquet`（5,955 bars，SHA-256 `4cb891...7b8`）→ Slurm run `run_20260723T151419Z_bdc5e5a0` / job `568306` → `cu16` 单节点 12 CPU、20 步、墙钟 `00:01:25`、累计 CPU `10:09.454`、峰值 `1254524K` → 最优分数 `0.8649603724` → replay 回测 → 通达信 `BUY`、虚拟仓位 `64.63%`。
- 01/02/03 真实浏览器验证中控制台、页面和 HTTP 错误均为 0。
- 对抗修复后的定向测试 143 通过、0 失败。完整非实盘套件 597 通过、17 失败；其中 16 个是既有陈旧合同测试，另 1 个是 Hypothesis 200ms 时限波动且单独重跑通过，本轮新增链路没有对应失败。
- AI 大模型接入是 AlphaMaster 的 fork 定制边界。用户已明确授权把当前 AI provider/前端源码和对应测试一起纳入本次精确快照；`.env`、真实 `web_settings.json`、浏览器/客户端会话、token 和其他运行态继续排除。
- 当前优先级重新冻结为 AM 自身的历史成绩真实性：继续审计数据对齐、特征/算子因果性、信号—目标收益—PnL 时间对齐，以及反复试验造成的回测过拟合。
- 2026-07-20 已修复并随 `67e81f3` 推送：多品种时间交集不足时仅做 `ffill()`，统一裁掉没有历史报价的前段；裁剪后不足 `MIN_BARS` 会失败关闭。定向单元 11 通过、时间轴属性 1 通过，独立六维审查 `PASS`。
- 2026-07-20 已修复：`model_core/backtest.py::_turnover_quality()` 不再对 `tanh` 连续仓位执行 `int(p)`，改按正负方向区间计数；同向加减仓保持一个持仓区间，空仓进场、平仓后再进场和方向翻转才新增区间。该 F02 语义是本 fork 的有意修复，后续选择性上游同步不得覆盖它。定向回归与回测契约测试共 42 项通过；项目 `.venv` 仍受 PyTorch `c10.dll` 的 `WinError 1114` 影响，结果来自隔离测试环境。
- 高优先待审查：训练循环反复使用 walk-forward validation 分数更新冠军、精英池和搜索方向，因此这些 validation 折更接近 selection 数据，不应在没有独立密封测试证据时直接称为最终样本外成绩。
- 2026-07-24 合成序列已确认：真实目标函数构造 `target_ret[t] = log(open[t+2]/open[t+1])`；当前 IC 额外右移并受尾部补零污染。成交、PnL 乘法与目标构造仍是分段证据，完整端到端时钟回归待生产修复时补齐。

## 本机数据状态

- 2026-07-16 已从当前登录的 MT5 精确品种 `NVDA` 导出 50,000 根已收盘 M5 K 线到 `local_data/MT5_K线数据/AlphaMaster_verified_20260716_142959/`；Parquet 与 manifest 配套，SHA-256 为 `3e2f634780ba3f18505d9c3df6654b78451e8b34d403f80d6f8e24594ffb67b6`，远程训练数据合同校验通过。该目录属于本机原始行情数据，不进入 Git；本次未启动训练。
- `local_data/MT5_K线数据` 指向 `E:\QQ文件\自动挖掘因子项目\MT5_K线数据`。只读批量注册计划扫描 668 个旧文件：537 个可注册、125 个不足 3000 bars、6 个含非法 OHLC / volume；计划保存在 `scratch/legacy_mt5_bulk_plan_20260716.json`，尚未 apply，原目录未新增 sidecar。
- 现有 `local_data/BTCUSDT_H1.parquet` 属于旧 OKX 归档数据，身份为 `okx_legacy_attested`；新版下载器仅保留 OKX `confirm=1` 的已完成 K 线，并生成 `okx` sidecar。

## 已知关键决策与约束

- 登录节点只作 SSH 跳板；远端计算入口在指定计算节点中选择，训练节点由 Slurm 调度。
- 12 CPU 已有大 A 同口径短任务基准：600519 D1 的 20 步作业墙钟 `00:01:25`、累计 CPU `10:09.454`、峰值内存约 1.2 GB。该基准只用于逐步扩大预算，不代表 9000 步可线性外推。
- 项目级 Stitch 配置只引用 `ALPHAMASTER_STITCH_API_KEY` 环境变量，不保存明文密钥。
- 训练包导出 v2 已实现并审查；Web/API 导入入口仍固定关闭，不得宣称用户可导入。
- manifest 不是 AlphaMaster 算法本地读取 Parquet 的固有要求，但所有模型训练均走 Slurm，因此训练输入必须有 sidecar。旧 MT5 / OKX 分别登记为 `mt5_legacy_attested` / `okx_legacy_attested`，不得伪装成新版导出器验证数据。
- 回测允许训练数据与测试数据哈希不同，但必须保持品种、周期、来源族和列合同一致；默认从训练结束后的第一根可用 K 线开始样本外评分，评估数据负责回测年化。
- Slurm run、Worker 结果、策略 JSON 和发布指针必须同时匹配 `data_rows`、`data_start`、`data_end`、`columns`；任何结果或策略范围篡改都不得进入 `READY`。
- AlphaMaster 是完全独立的项目。不得把其他仓库的目标、上下文、源码、任务、提交、文件包或执行流程混入本项目。
- 当前发布基线没有券商订单执行通道；实盘是后续主线，但必须逐市场、逐工单完成模拟盘/风控/监控与用户授权后才能接真实账户或发单。
- 当前公式兼容版本为 `v9217a2c0d91a`；仓库内更早版本的历史策略仅保留作旧证据，不能续训、回测或实时使用，后续必须基于当前语义重新训练，不做静默迁移。

## Canonical 移交入口

- 项目规则：`docs/CODEX_PROJECT_RULES.md`
- 本机环境与 skills/memory 边界：`docs/LOCAL_EXECUTION_CONTEXT.md`
- 用户问题、需求、修改意见和完成记录：`docs/REQUIREMENTS_CHANGELOG.md`
- 当前验证证据：`docs/VALIDATION_EVIDENCE.md`（顶部为 2026-07-23 现役闭环，后文保留历史证据）
- 多标的组合控制：`docs/PORTFOLIO_CONTROLLER_V1.md`
- 网页版 GPT / BioMNI 组合审查指令：`docs/BIOMNI_PORTFOLIO_REVIEW_PROMPT.md`
- GPT-5.6 移交说明：`docs/GPT5_6SOL_HANDOFF.md`
- 历史实盘方向交接：`docs/LIVE_TRADING_DIRECTION_HANDOFF.md`（已被当前大 A 主线取代）
- 网页版 GPT 总控指令：`docs/WEB_GPT_CONTROLLER_PROMPT.md`
- 历史未来函数专项总控指令：`docs/GPT_WEB_PRO_EXTENDED_TASK.md`

## 下一步

保持恢复批次 `batch_8e173c5a667a4b31bd8573c760842b70` 连续运行，并逐只记录真实 Slurm 资源、训练产物、成本 replay 和虚拟信号；当前继续监控 `002475` / Slurm `569604`。多标的组合控制 v1、运行提交冻结修复和数据源选型当前等待精确提交与推送；公开 `main` 更新后，再把最新公开提交和证据包交给网页版 GPT 与 BioMNI。本机不得提前揭示最终封存集，也不得把当前中证 A50 基线或旧四股诊断记录冒充最终轮动结果。

2026-07-22：`web/slurm_training_client.py` 的远端命令选择器已改用 PowerShell 7 (`pwsh.exe`)，相应单元测试会断言真实选中的可执行文件名；定向测试 `13/0`。这只影响本机发起 Slurm 远端命令的宿主，不改变登录节点、计算节点或 Slurm 调度边界。
