# CONTEXT

## 当前目标

当前以普通大 A 账户为第一落地方向。用户没有可验证的 QMT/PTrade/目标券商实盘渠道，因此本阶段不接券商、不发单；先贯通“服务器 Slurm 模型训练 → 成本回测 → 通达信已收盘 K 线虚拟信号 → 可选飞书推送”。Windows 本机只负责数据准备、控制、回测、信号与前端，所有模型训练一律提交服务器 Slurm，禁止本机训练。

当前提交不接入真实账户、不发单、不保存交易凭据。大 A 语义固定为普通账户只做多；负因子在空仓时观望，在虚拟持仓时转为减仓或离场目标。网页版 GPT 只读取 AlphaMaster 的已提交 GitHub 快照，充当总控和工单设计者；本机 Codex 才能在真实环境中复核并执行。

## 2026-07-24 A50 龙头基线与持续训练

- 当前先用 2026-07-23 冻结的中证 A50 官方成分打通 50 只串行工程链。它是高流动性、跨行业龙头候选池，不是“近一年轮动表现最强 50 只”的最终筛选结果；证据与缺口见 `docs/A50_ROTATION_RESEARCH_EVIDENCE.md`。
- 50/50 只新浪后复权日线已下载并冻结；训练数据统一截止 2025-07-23，2025-07-24 至 2026-07-23 作为物理隔离的最终封存窗口。成分合同 SHA-256 为 `987387945fba0cb778b648860bc7579a3cf49e9c3b788596464f714e968bb896`，切分合同 SHA-256 为 `ec5a9549587802b3f0f82fd566d953315b3ecbf0a42d24f21795a8978a60cf73`。
- 持久队列 `batch_a50_26e3472c68ca5c04` 已启动，共 50 项、每项 200 步、单节点 12 CPU、32 GB、30 分钟；批次冻结训练源码 SHA-256 `02350ba4e3c696461f2be9f042cb00912568442b3f9986151a79621f571eeb8d`。第一项 `000333` / run `run_20260723T235959Z_908fc2fc` / job `568571` 已完成服务器训练、成本 replay 和虚拟信号，队列状态为 `READY`。训练分数 `1.2064923048`，同数据含成本 replay 夏普 `0.7105`，后者不是样本外成绩。
- 第二项 `000617` / run `run_20260723T235959Z_f4a3f24c` / job `568740` 已完成；单节点墙钟 `00:11:27`，训练分数 `1.2361105680`，同数据含成本 replay 夏普 `0.6114`，最新虚拟动作 `BUY`、目标仓位 `32.57%`。第三项 `000725` / run `run_20260723T235959Z_2d4aa4b4` / job `568756` 也已完成；单节点墙钟 `00:11:21`、累计 CPU `01:30:54`、峰值内存 `1280816K`，训练分数 `1.4638690948`，同数据含成本 replay 夏普 `1.0163`，最新虚拟动作 `BUY`、目标仓位 `40.19%`。这仍不是样本外成绩。
- 第四项 `000792` / run `run_20260723T235959Z_8bf9a46f` / Slurm job `568779` 已在 `cu06` 单节点完成；墙钟 `00:08:57`、累计 CPU `01:06:16`、峰值内存 `1264620K`，训练分数 `1.9909664392`，同数据含成本 replay 夏普 `0.9750`、累计对数收益 `3.704436`、287 笔，最新虚拟动作 `BUY`、目标仓位 `21.65%`。第五项 `000988` / run `run_20260723T235959Z_350644f9` / job `568800` 已提交，2026-07-24 最新查询为 Slurm `PENDING (Priority)`；页面与持久队列为 `4/50`、1 项活动、45 项待训练、0 项需处理，作业没有被取消或中断。
- 多标的组合控制层 v1 已落地：完整冻结股票池合同 → 752 根新浪后复权同源日线 → 各历史时点独立 500 根窗口重放 → 252 点单股分位校准 → Top-K + Dropout → 等权目标与 SQLite 审计。A50 正式入口只接受同一交易日、同一 `1d` 周期、同一上海 15:00 收盘时点和同一 `akshare_sina_hfq_ohlcv` 来源；4/50 或 50 只一致使用错误周期/来源都不能冒充正式 A50。
- 单股流水线升级为 `alphamaster_signal_simulation_v3`，先冻结市场输入再写账本；崩溃重试不能换交易日。后处理增加跨进程唯一租约；组合正式入口从 READY 状态、run manifest、回测、发布策略、冻结输入和输出哈希内部生成身份，不接受调用方自报 expected。信号判断只读完整精度的 `position_raw`、`strength_raw` 和 `factor_value_raw`，四舍五入字段仅供展示；组合账本读回时重算 `decision_id` 并绑定 SQLite 主键和 K 线时间。
- Web 只读状态接口改为本机快照，不再让浏览器轮询阻塞 SSH；状态、日志和步数按同一 run 绑定，终态会补拉最后日志。前端状态读取失败时保留上次可信快照并禁用训练操作，不再误显示空闲；活动批次优先按真实 `active_item` 选择；停止操作必须同时携带并匹配预期 run/job。
- 已修复首项后处理与独立对抗审查暴露的控制层问题：不可变策略产物允许省略冗余 `run_id`，但显式错误 `run_id` 必须失败；训练已经 `READY` 时，永久后处理失败不能被历史重试状态重新启动；停止请求绑定旧 run/job 时返回冲突；批次在真正提交前再次核对冻结源码；损坏或身份不明的本地状态失败关闭为 `RECOVERY_UNKNOWN`，不能显示空闲或启动新 run。现役作业没有重训、重提或取消。
- pytest 在导入 Web 管理器前同时隔离 `local_runs` 和训练队列。四只已完成股票的真实新浪 752 根日线与冻结全量 Parquet 的时间、float32 OHLC 和成交量逐项一致；`000333` 已用真实公式完成 252 点滚动校准。组合、流水线、行情、账本与控制链定向回归 `226/0`；完整非实盘回归 `816/16`，16 项仍为既有陈旧合同，本轮新增链路没有失败。
- 多标的组合 v1 的三路独立对抗审查均已 PASS。最后一路亲自重放 8 条精度、A50 来源/周期和账本篡改攻击，`8/8` 均被拒绝，定向复验 `70/0`，且没有调用 SSH/Slurm 或触碰真实训练队列。
- 为保证训练不断，本轮没有重启现役 Web/批队列进程；组合 v1 仍是本地已验证候选，未来受控重启后才进入长驻进程。当前旧进程继续监控并推进 `000988` / Slurm `568800`。
- 封存评估审查边界：前述 5 个控制层 P1 来自已经完成的独立对抗审查并已修复；针对最后一处“同一批密封字节改标签或改窗口再次揭盲”的增量修复，两次追加子 Agent 均未在限定时间内返回结果，因此当前证据是主 Agent 手工调用顺序审查、反例测试和机器回归，不能声称该最后增量已经独立复核 PASS。
- 用户的“50 只每只扣费后样本外 Sharpe > 1”是最终整批门槛，不是允许反复查看封存集的调参规则。首轮 200 步训练和同数据 replay 只用于跑通与筛选，最终必须冻结 50 个策略后一次性揭示统一封存窗口；当前没有任何一只可被声称已经通过最终门槛。
- 封存评估合同已升级为 v2：合同和逐只报告必须同时绑定严格大于 0 的手续费、滑点和实际 `cost_rate`；报告声明的成本率必须与回测实际使用值一致。一次性揭盲锁只按“50 份密封文件 SHA-256 的排序多重集合”生成数据集身份，落在当前系统用户的全局注册目录；股票代码标签、测试窗口、合同路径、结果路径和策略 SHA 都不参与锁身份，因此同一批物理字节换标签、换窗口或换策略仍会命中同一锁。封存与回测合同定向测试 `74/0`。

## 2026-07-19 当前方向与上游同步边界

- 用户确认采用“选择性移植”，永不整体 merge 上游：量化、金融、模型训练与通用数据处理优先保持上游逻辑；AI 大模型接入、Slurm 训练调度、前端页面和实盘接入方向必须保留。
- 本轮三张上游同步工单已在本地工作树完成：StackVM/JUMP 改为上游 expanding 标准化并删除 formula contract；通达信指数改走指数接口；Parquet 文件名入口支持常见周期别名。
- 本轮已完成本地范围测试、真实通达信请求和独立六维复验。2026-07-20 用户明确授权将当前公开仓库作为临时发布目标；36 路精确快照已随 `4a897de` 推送到 `main`，`.env`、真实配置、密钥、数据、模型和日志未上传。
- 不移植上游的数据驱动年化估算、秒/毫秒静默修复、tqsdk 凭据/期货实现、前端重试和 AI provider 微调，除非用户为相应区域另开工单。

## 稳定基线

- 目标发布仓库：`https://github.com/xiaojinlucky/AlphaMaster`，分支 `main`，上游为原作者仓库。用户已于 2026-07-24 明确要求仓库持续保持公开，以便网页版 GPT 与 BioMNI（BIOMNI）直接读取源码和证据；不得再把改为私有作为发布前提。公开不放宽精确暂存、密钥扫描和运行态排除规则。
- 本轮选择性上游同步开始前的本地与远程功能基线：`e747eadf78f95777315ea961671c23491d116a7e`；最终交接 SHA 由 GitHub `main` 动态读取，不在提交内自我硬编码。
- 前端三页统一完成于 `186217ecb7c9658187b8a335c1445af576263f02`；相关视觉合同测试 21 项通过，01/02/03 已统一大字体、舒展排版、侧边栏、卡片和控件基线。
- `093e317` 已将 `times.py` 的 Tushare 明文 token 改为只读取本机 `TUSHARE_TOKEN` 环境变量；但旧 Git 历史仍含旧值和历史训练产物，必须先轮换 token，且不得把 `.git` 交给外部模型。

## 已完成功能

- A 股旧数据严格转换与统一数据来源合同。
- 旧 MT5 两阶段可审计登记；新 MT5/OKX 自动 sidecar。
- `timeframe + data_sha256 + run` checkpoint 隔离。
- 策略、checkpoint、Slurm run/result、发布指针和训练包完整身份贯穿。
- 回测独立测试数据、replay / out-of-sample / diagnostic-overlap 和评估数据年化。
- 训练包 v2 路径、成员、大小、CRC、哈希、身份与失败回滚校验。
- 历史 200 根滚动标准化与公式执行合同的修复证据仍保留在 Git 历史和验证文档中；用户随后决定以原作者上游的 expanding 标准化为当前语义，因此运行时代码已删除 `formula_contract.py`。
- 当前 `StackVM._normalize_output()` 与 `JUMP` 使用 expanding 统计，`VOCAB_VERSION` 只由有序 token 名称派生；旧版带执行合同的产物版本不再匹配当前词表版本。
- 主线程相关回归 140 通过、0 失败；独立审查扩大回归 229 通过、0 失败；完整 unit 444 通过、8 个既有失败。
- 当前受影响公式、数据与 Web 契约测试 151 通过、0 失败；AI provider 定向测试 28 通过、0 失败。完整 unit 为 513 通过、9 个既有失败；排除本轮相关测试后为 402 通过、相同 9 个失败。
- 通达信真实请求已验证 `sh000001` 与 `600519`；`drop_forming=True` 只会移除未来未形成 K 线，不会丢掉最新已收盘 K 线。
- 普通大 A 只做多信号生命周期、SQLite 幂等虚拟仓位、飞书重试与前端信号历史已完成；动作覆盖买入、加仓、减仓、离场、止盈、止损和不打扰的持有/观望。
- `web/a_share_pipeline.py` 已把同一大 A run 的 Slurm 训练、replay 回测和通达信虚拟信号串成自动后处理；后台监控不依赖浏览器轮询，外部行情/回测超时最多重试 3 次，身份或合同错误立即失败。
- 后处理只读取 `published_training/current_<symbol>.json` 指向且逐项哈希通过的 Slurm 发布包，并同时匹配 run、数据、结果清单和产物根目录；可变的兼容策略副本不能再冒充当前训练结果。
- `AlphaEngine.train()` 和 `train_file.train_from_file()` 均要求 Linux Slurm Worker 运行标记、Slurm job ID 与计算节点；Windows 本机和普通命令行会在读取训练数据前失败。
- 首个真实闭环：`600519_D1.parquet`（5,955 bars，SHA-256 `4cb891...7b8`）→ Slurm run `run_20260723T151419Z_bdc5e5a0` / job `568306` → `cu16` 单节点 12 CPU、20 步、墙钟 `00:01:25`、累计 CPU `10:09.454`、峰值 `1254524K` → 最优分数 `0.8649603724` → replay 回测 → 通达信 `BUY`、虚拟仓位 `64.63%`。
- 01/02/03 真实浏览器验证中控制台、页面和 HTTP 错误均为 0。
- 对抗修复后的定向测试 143 通过、0 失败。完整非实盘套件 597 通过、17 失败；其中 16 个是既有陈旧合同测试，另 1 个是 Hypothesis 200ms 时限波动且单独重跑通过，本轮新增链路没有对应失败。
- AI 大模型接入是 AlphaMaster 的 fork 定制边界。用户已明确授权把当前 AI provider/前端源码和对应测试一起纳入本次精确快照；`.env`、真实 `web_settings.json`、浏览器/客户端会话、token 和其他运行态继续排除。
- 当前优先级重新冻结为 AM 自身的历史成绩真实性：继续审计数据对齐、特征/算子因果性、信号—目标收益—PnL 时间对齐，以及反复试验造成的回测过拟合。
- 2026-07-20 已修复并随 `67e81f3` 推送：多品种时间交集不足时仅做 `ffill()`，统一裁掉没有历史报价的前段；裁剪后不足 `MIN_BARS` 会失败关闭。定向单元 11 通过、时间轴属性 1 通过，独立六维审查 `PASS`。
- 2026-07-20 已修复：`model_core/backtest.py::_turnover_quality()` 不再对 `tanh` 连续仓位执行 `int(p)`，改按正负方向区间计数；同向加减仓保持一个持仓区间，空仓进场、平仓后再进场和方向翻转才新增区间。该 F02 语义是本 fork 的有意修复，后续选择性上游同步不得覆盖它。定向回归与回测契约测试共 42 项通过；项目 `.venv` 仍受 PyTorch `c10.dll` 的 `WinError 1114` 影响，结果来自隔离测试环境。
- 高优先待审查：训练循环反复使用 walk-forward validation 分数更新冠军、精英池和搜索方向，因此这些 validation 折更接近 selection 数据，不应在没有独立密封测试证据时直接称为最终样本外成绩。
- 待合成序列确认：PnL 使用 `position[t] * target_ret[t]`，但 IC 使用 `factor[t]` 对齐 `target_ret[t+1]`；必须证明两者预测窗口是否一致。

## 本机数据状态

- 2026-07-16 已从当前登录的 MT5 精确品种 `NVDA` 导出 50,000 根已收盘 M5 K 线到 `local_data/MT5_K线数据/AlphaMaster_verified_20260716_142959/`；Parquet 与 manifest 配套，SHA-256 为 `3e2f634780ba3f18505d9c3df6654b78451e8b34d403f80d6f8e24594ffb67b6`，远程训练数据合同校验通过。该目录属于本机原始行情数据，不进入 Git；本次未启动训练。
- `local_data/MT5_K线数据` 指向 `E:\QQ文件\自动挖掘因子项目\MT5_K线数据`。只读批量注册计划扫描 668 个旧文件：537 个可注册、125 个不足 3000 bars、6 个含非法 OHLC / volume；计划保存在 `scratch/legacy_mt5_bulk_plan_20260716.json`，尚未 apply，原目录未新增 sidecar。
- 现有 `local_data/BTCUSDT_H1.parquet` 属于旧 OKX 归档数据，身份为 `okx_legacy_attested`；新版下载器仅保留 OKX `confirm=1` 的已完成 K 线，并生成 `okx` sidecar。

## 已知关键决策与约束

- 登录节点只作 SSH 跳板；远端计算入口在指定计算节点中选择，训练节点由 Slurm 调度。
- 12 CPU 已有大 A 同口径短任务基准：600519 D1 的 20 步作业墙钟 `00:01:25`、累计 CPU `10:09.454`、峰值内存约 1.2 GB。该基准只用于逐步扩大预算，不代表 9000 步可线性外推。
- 项目级 Stitch 配置只引用 `ALPHAMASTER_STITCH_API_KEY` 环境变量，不保存明文密钥。
- 训练包导出 v2 已实现并审查；Web/API 导入入口仍固定关闭，不得宣称用户可导入。
- manifest 不是 AlphaMaster 算法本地读取 Parquet 的固有要求，但所有模型训练均走 Slurm，因此训练输入必须有 sidecar。旧 MT5 / OKX 分别登记为 `mt5_legacy_attested` / `okx_legacy_attested`，不得伪装成新版导出器验证数据。
- 回测允许训练数据与测试数据哈希不同，但必须保持品种、周期、来源族和列合同一致；默认从训练结束后的第一根可用 K 线开始样本外评分，评估数据负责回测年化。
- Slurm run、Worker 结果、策略 JSON 和发布指针必须同时匹配 `data_rows`、`data_start`、`data_end`、`columns`；任何结果或策略范围篡改都不得进入 `READY`。
- AlphaMaster 是完全独立的项目。不得把其他仓库的目标、上下文、源码、任务、提交、文件包或执行流程混入本项目。
- 当前发布基线没有券商订单执行通道；实盘是后续主线，但必须逐市场、逐工单完成模拟盘/风控/监控与用户授权后才能接真实账户或发单。
- 当前公式兼容版本为 `v9217a2c0d91a`；仓库内更早版本的历史策略仅保留作旧证据，不能续训、回测或实时使用，后续必须基于当前语义重新训练，不做静默迁移。

## Canonical 移交入口

- 项目规则：`docs/CODEX_PROJECT_RULES.md`
- 本机环境与 skills/memory 边界：`docs/LOCAL_EXECUTION_CONTEXT.md`
- 用户问题、需求、修改意见和完成记录：`docs/REQUIREMENTS_CHANGELOG.md`
- 当前验证证据：`docs/VALIDATION_EVIDENCE.md`（顶部为 2026-07-23 现役闭环，后文保留历史证据）
- 多标的组合控制：`docs/PORTFOLIO_CONTROLLER_V1.md`
- 网页版 GPT / BioMNI 组合审查指令：`docs/BIOMNI_PORTFOLIO_REVIEW_PROMPT.md`
- GPT-5.6 移交说明：`docs/GPT5_6SOL_HANDOFF.md`
- 历史实盘方向交接：`docs/LIVE_TRADING_DIRECTION_HANDOFF.md`（已被当前大 A 主线取代）
- 网页版 GPT 总控指令：`docs/WEB_GPT_CONTROLLER_PROMPT.md`
- 历史未来函数专项总控指令：`docs/GPT_WEB_PRO_EXTENDED_TASK.md`

## 下一步

保持 `batch_a50_26e3472c68ca5c04` 连续运行，并逐只记录真实 Slurm 资源、训练产物、成本 replay 和虚拟信号。多标的组合控制 v1 已完成本地复审，当前等待用户确认精确提交与推送；公开 `main` 更新后，再用内置浏览器把最新公开提交、`docs/A50_ROTATION_RESEARCH_EVIDENCE.md` 和 `docs/BIOMNI_PORTFOLIO_REVIEW_PROMPT.md` 交给网页版 GPT，并把同一指令交给 BioMNI，设计“最终 50 只筛选 + 虚拟组合执行 + 智能调控”后续工单。本机不得提前揭示最终封存集，也不得把当前中证 A50 基线或旧四股诊断记录冒充最终轮动结果。

2026-07-22：`web/slurm_training_client.py` 的远端命令选择器已改用 PowerShell 7 (`pwsh.exe`)，相应单元测试会断言真实选中的可执行文件名；定向测试 `13/0`。这只影响本机发起 Slurm 远端命令的宿主，不改变登录节点、计算节点或 Slurm 调度边界。
